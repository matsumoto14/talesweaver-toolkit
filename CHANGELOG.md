# 変更履歴

このツールを使う人向けの変更点です。設計上の決定は [docs/adr/](docs/adr/) にあります。

書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。
`1.0.0` は「ダメージ計算・キャラ管理・強化提案・ロードマップ・索引の 5 機能がそろい、
ゲーム内の実測値と突き合わせて検証できた」時点にします。

## [未リリース]

### 追加

- **ブラウザで使えるようになりました** — インストールせずに、リンクを開くだけで
  同じ画面をそのまま使えます。キャラの登録もダメージ計算もできます。
  データは**そのブラウザの中**に入るので、別の端末や別のブラウザからは見えません。
  ブラウザの閲覧データを消すと一緒に消えるため、**情報パネルから書き出し / 読み込み**
  (JSON ファイル 1 つ)ができるようにしました。デスクトップ版との行き来もこのファイルで行えます
- **実測タブ** — ゲームで実際に出たダメージを、条件ごと送れます。収録済みの敵なら
  このツールの計算との差がその場で出ます。**まだ収録していない敵(新しいモブ)**も、
  名前を書いて送れます。**測るときの武器をこの画面で切り替えられる**ので、キャラの登録を
  触らずに攻撃力を変えた点を何点でも記録でき、まとめて 1 通で送れます
  (2 点以上あると敵の防御力とカット率を分けて逆算できます)。
  敵の防御力・カット率は wiki でも「約」「推定値」で、集めた実測から逆算するしか
  確かめる方法がないためです
- **計算タブの「なぜこの数字?」で、切り替えがどこに効いたかを辿れます** — 称号や装備を
  切り替えると、内訳の供給源に「追加」「削除」の行が残り、閉じたカテゴリ行にも
  「+ 称号【…】」の注記が出ます。緑の ↑ を押すと、その下で変わった行だけが順に開きます。
  内訳を閉じたまま切り替えても、開けば差分が残っています

### 変更

- **オン / オフの操作を 1 つの形にそろえました** — バフ・極限スキル・キャラスキル・
  ペット会心などの「効かせる / 効かせない」は、どの画面でも同じ「行チップ」になりました。
  行全体を押すと切り替わり、オンは水色(保存される)/ ラベンダー(この画面だけ)、
  オフは白のまま値だけ薄く残ります。チェックボックスとチェック印のチップはなくなりました
- **文字を入れる欄を 1 つの形にそろえました** — キャラの呼び名・装備やオーラの登録名・
  装備 / 称号の検索・実測のメモ・問い合わせの件名と内容は、どの画面でも同じ高さ・角丸の欄になりました。
  呼び名と新しいバフセットの名前は空欄ではなく、キャラ名や「セット n」が自動で入り、押したときだけ書き換えられます。
  検索欄は左の ⌕ と右の件数で「いま何件から探しているか」が常に見え、文字数に上限のある欄は右端に「n/上限」が出ます。
  薄い文字で例を示す placeholder はなくなりました
- **「1 つ選ぶ」欄を 1 つの形にそろえました** — いつものバフ・計算タブの使うセット・主軸スキル・
  武器アビリティ・研磨の種類と乗せる先・装備の切り替え・称号・カタログ外装備の種別は、
  よく使う候補がチップで手前に並び、残りは「ほか n 件」を押すと重なって出る候補面から選べます。
  候補にはどれも選ぶのに要る値(段数・属性・増える量・件数など)が付き、「なし」「未選択」も候補の 1 行です。
  ドロップダウンはなくなりました
- **数を入れる欄を 1 つの形にそろえました** — ホームのエンチャント欄は、キャラタブの装備と同じ
  「値 /上限」のセル(底のバーで上限までの位置が見えます)と +12 / +14 / +17 / +20 / MAX になりました。
  実測タブの「何発中の最大」は − / ＋ で動かせます。上限のない「実測ダメージ(1 発)」だけは
  青い枠と「実測値 · 一時」の破線チップが付き、手で入れる欄だと分かるようにしました。
  素の数値入力欄はなくなりました
- **読み取りの面と数字の見せ方を 1 つの形にそろえました** — 防御パネル・ホームのステータス・
  対人の材料・バフの要約・実測の差は、どの画面でも同じ「ラベル + 右端の値」の行と、同じ色の
  読み取り面(水色のインセット)になりました。未収録・未登録の印は破線のバッジ 1 種類になり、
  減る値の符号はどこでも「−」(マイナス記号)で、「%」の丸め方も画面ごとに違わなくなりました。
  実測表の中ディレイ基本値は 1.00s のように 2 桁で出ます
- インストーラのアイコンをアプリのアイコンにしました(これまでは NSIS の既定アイコンでした)

### 修正

- コンボ時の「1 秒あたり」の内訳で、通常攻撃のダメージだけクリティカルでない側を出していたため、
  合計と足し算が合わないように見えていました(1 秒あたりの数値そのものは正しいままです)

## [0.1.1] — 2026-08-31

### 追加

- **更新を確認** — お知らせタブから、その場で新しい版があるか確かめられるようになりました。
  起動したときに通信できなかった場合でも、あとから自分で確認できます

## [0.1.0] — 2026-08-31

最初の公開版。

### できること

- **キャラクター管理** — 名前とキャラ種だけで登録し、素ステータス・覚醒・エタの意志・
  装備(部位別 12 スロット)・シエナのオーラ・テシスコア・称号・ランダムオプション・
  ルーン・クラウン・カード・ペット・共通スキル・マスタリーまでを編集できます
- **ダメージ計算** — キャラ・スキル・コンテンツを選ぶだけで、1 発 / 合計 / クリティカルと
  1 秒あたりの火力(DPS)を表示します
- **コンボの 1 秒あたり** — コンボは間に通常攻撃を挟むので、その通常攻撃ぶんの時間と
  ダメージも入れた 1 サイクルで火力を出します(挟む通常攻撃は選べます)
- **なぜこの数字?** — 攻撃力の内訳、相手の防御をどれだけ抜けているか、どの倍率が効いているかを
  展開して確認できます。全 30 カテゴリの計算過程もすべて追えます
- **もし〜だったら** — 装備やステータスを仮に変えたときのダメージ差を、保存せずに試せます
- **ホーム** — どのコンテンツに入れるか、何が足りないかを一覧できます
- **お知らせ** — 更新内容・これから実装するもの・既知の不具合をタブで見られます。
  中身は配信されるので、アプリを入れ替えなくても新しいお知らせが届きます
- **自動更新** — 新しいバージョンが出ると起動時に気づけて、お知らせタブのボタンひとつで
  入れ替えられます(勝手に落として当てることはしません)
- **問い合わせ** — アプリから直接ご意見・不具合報告を送れます
- **情報パネル** — 画面右上から、バージョン・データの出典・扱いを確認できます

### データの守り方

- データベースを更新のたびに自動でバックアップします。万一読み込めなくなっても、
  直前のバックアップから復元して起動します
