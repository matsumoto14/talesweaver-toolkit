# 変更履歴

このツールを使う人向けの変更点です。設計上の決定は [docs/adr/](docs/adr/) にあります。

書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。
`1.0.0` は「ダメージ計算・キャラ管理・強化提案・ロードマップ・索引の 5 機能がそろい、
ゲーム内の実測値と突き合わせて検証できた」時点にします。

## [未リリース]

### 変更

- アナイスの陣(テスラコイル・アイスエイジ・フレアフィールド)を「置いたら持続が切れるまで別の技を連打し、切れたら置き直す」形でスキル回しに数えるようにしました。主軸がアングリーピクシーなら既定でテスラコイルが差し込まれます

## [0.7.0] — 2026-09-22

### 変更

- 内部の準備だけの版です。画面に出る変更はありません

## [0.6.0] — 2026-09-21

### 変更

- **問い合わせに付けられるデータが「装備」から「キャラのデータ一式」になりました** — 装備に加えて、
  素ステ・ペットやルーンなどの補正源・共通スキル・計算に使っているバフ・アプリが出していた数字を
  一緒に送れます。「計算が合わない」の報告を、こちらで同じ数字まで再現できるようになります。
  キャラ名・ラベル・バフセット名は含めません(これまでどおり、押した人だけ・送信前に全文を確認できます)

### 修正

- **チャネリング技の DPS が実際の約 1/10 で出ていました** — 極・連撃 などの「486%x10(1 秒毎)」は
  1 秒ごとに 10 段で、最大 10 回くり返します。これを「10 段を 10 秒に 1 回」と数えていました。
  対象は 16 技(極・連撃 / 極・無双乱舞 / 極・旋風斬 / 極・狂猫 / 極・双龍撃 / 極・紅龍連撃 /
  極・滅神乱舞 / 極・ブリザード / 極・アイシクルレイン など)で、DPS・討伐時間・主軸スキルの並びが
  変わります。1 発と合計ダメージの表示はゲーム内と同じ 1 回ぶんのままです
- **チャネリング技(極・連撃、極・スパークリングカイトなど)に中ディレイ減少が効いていませんでした** —
  2025/10/29 の仕様変更どおり、中ディレイ減少のぶん 1 回の使用が短くなり、DPS が上がります
  (フルスロットル最大で約 1.8 倍)。同じ変更で廃止された「フルスロットルの単体チャネリング段数 +1〜+3」は
  外しました。該当の 7 技は 1 回あたりの合計ダメージが下がります
- **装備の「装着時与ダメージ+3%」などが計算に入っていない装備がありました** — †フレンの籠手・
  †エステルの武醒魔導器・†血晶武装の武器・コラボのアーティファクトなど 20 点で、装備しても
  効果ぶんのダメージが増えていませんでした
- **†エーテリアルチューブ(通常版)の依存ダメージ +35% を入れました** — これまで数値が分からず
  未計算でした。物理力以外の 5 種は同じ系列の値に合わせた仮の値です
- **迅速の秘薬がバフタブのどこにも出ていませんでした** — 0.4.0 で追加したものの、全属性 +15 しか
  持たないバフを火力タブが拾っていませんでした。「火力を上げたい → その他」に出ます
- **イェフネンの速剣・鋭い欠片・べたつく欠片が全部の技に掛かっていました** — 速剣はソード系の
  4 技だけ(倍率 0.9 倍・攻撃回数 +1)、欠片は <フラグ> だけに効くのが正しい仕様です。ON にしていた
  場合はダメージが変わります。欠片 2 つの項目は無くなり、マスタリー 3 の選択で自動で入ります
- **スキルの SLv(極・的中剣など)が起動のたびに最大へ戻っていました**

### 追加

- **DPS を「連打する技 + 差し込む CT 技」のスキル回しで出すようになりました** — クールタイムのある技は
  CT ごとに 1 回、空いた時間は連打する技で埋めて DPS・討伐時間を出します。これまでは CT のある
  大技を連打できる前提で出していたため、主軸にすると過大でした。1 発の数字は変わりません。
  ホームの「行ける / 行けない」・強化候補・主軸スキルの並び(継続火力の目安)も同じ計算になります
- **キャラタブで「差し込む CT 技」を選べます** — 主軸スキルの下のチップで選びます(全キャラの CT を
  収録)。最初は、差し込むと DPS が上がる技のうち 1 回の火力が一番高いものが入っています。
  差し込むと DPS が下がる技は「DPS が下がる技」の中に畳んであり、開けば選べます
- **ダメージ計算タブに「スキル回し」の段を足しました** — 1 周の流れ(CT 技 → 合間に連打 × 何回)を
  秒の目盛り付きで見られ、技ごとの DPS の内訳も出ます。差し込む技はその場で ON / OFF して
  試せます(計算タブでの変更は保存されません)

- **イェフネンの <フラグ> を計算に入れました** — 「このキャラのスキル」でスタック数(0〜10)を
  入れると、スレイ / クラッシュ の合計に爆発ぶんが、DPS に持続ダメージが入ります。DPS は
  連 / 爆 で積み直す時間と CT 10 秒を含めたスキル回しで出します
- **イェフネンの主軸スキルを「形態 → 技」の順に選べます** — その形態で意味のある入力(速剣・
  最大までチャージ・後方から攻撃)だけが下に出ます。ブレンドは 0〜10 スタックで入力できます

- **バフをキーワードで探せます** — バフタブの検索欄に名前や効果の一部を入れると、目的のタブを
  またいで全バフから探します

- **エフェクトを中心に装備を 49 点追加しました** — †千尋の血晶武装 など昔のコラボエフェクトも選べます。
  カスタム装備で作っていた場合は、カタログの装備に選び直すと「攻撃力 3% 上昇」が計算に入ります

## [0.5.1] — 2026-09-21

### 修正

- **異界の峡谷防衛戦のコアがルビコナになっていました** — エクリプスが正しく、侵入条件の
  「コア 300」も中で効くコアもエクリプスコアです。エクリプスコアがそろっているのに
  「あと 254 必要」と出ていたのを直しました
- **軽鎧などの突き・斬りにエンチャント欄が出ませんでした** — †カスミの着物のように上限が 255 の
  補正を「上限が分かっていないもの」と誤って扱い、エンチャント枠を 0 にしていました。255 まで
  伸ばせるようになります。武器・鎧・腕あわせて 196 点の上限が開きます。もともと数値を持たない
  補正(ハンマーの斬りなど)は、これまでどおりエンチャントできません
- **†スタリオンサインのサブ補正の上限を直しました** — 一律 255 としていましたが、主能力と対になる
  能力(ブルーなら斬り)は 500 が上限です。4 色すべての 9 値を入れ直しました

## [0.5.0] — 2026-09-21

### 追加

- **補正付きアバターを設定できます** — アイテム名の末尾に「Ａ」が付くアバターは、1 点で装備補正
  9 値すべてに +1、兜・頭・体・脚・エフェクトの 5 部位そろえるとセット効果でさらに +10 付きます。
  これまでアバター強化剤しか入れられず、各装備補正がゲーム内と最大 15 ずれていました
- **問い合わせに装備を付けられます** — 選択中のキャラの、装備中の装備・オーラ・コア・称号を一緒に送れます。
  キャラ名と自分で付けたラベルは含みません。既定では付かず、送る内容はこれまでどおり送信前に全文表示されます。

### 変更

- **アバターの画面を作り直しました** — 上が「効いている量」の表、下が入力の 2 段になり、9 つの補正が
  上下で同じ位置に並びます。どの補正を押すと合計がいくつ動くかがその場で見えます。強化剤は部位を
  開かなくても全部見えていて、枠を押すたび なし → +10 → +12 と変わります。主軸スキルが使う補正は
  少し濃く出ます

### 修正

- **[ブラウザ版] しばらく開いていないと、キャラの一部の設定が消えることがありました** — 何世代か前の
  ブラウザ版で保存したデータを開くと、保存先の作り直しが途中までしか適用されず、召喚スキルの選択・
  ホームの「次の目標」・ルミナの回廊が消えることがありました。まとめて正しく引き継がれます。
  デスクトップ版は影響を受けません

## [0.4.1] — 2026-09-20

### 修正

- **装備の数値がゲーム内と合わない** — キャラタブの装備一覧に出る数値が、アビリティのぶんだけ
  低く出ていました。ゲーム内の装備画面と同じ値になります
- **変えていない数値に ↑↓ が付く** — シャープネスビジョンの Lv を変えたときなど、実際には
  動いていない数値にも前の増減が残っていました。いま動いた数値にだけ付きます
- **増減の数字から中身をたどれない** — 増減の数字を押すと、その中で変わった項目まで開く
  ようになっていましたが、押せる見た目にならず効かなくなっていました。討伐時間が縮んだときに
  色が付かないのも直りました

### 変更

- **装備一覧に出す数値** — 使っているスキルに効く数値(斬りで戦うなら斬り、魔法斬りなら
  斬りと魔攻)を並べるようにしました。部位ごとに違う項目が出ていたのが揃うので、どの装備が
  どれだけ効いているか見比べやすくなります

## [0.4.0] — 2026-09-19

### 追加

- **「全属性 +15」のバフが計算に入るようになりました** — イルミネーション祭りのドリンク・
  ユキダルマン族の特製ポーション・迅速の秘薬の 3 つです(迅速の秘薬は新規追加)。いずれも
  wiki には全属性の記載が無く、公式お知らせが出典です。バフタブで ON にすると属性値に乗ります
- **補正源に「ルミナの回廊」が増えました** — 回廊効果のうち「最終ダメージ」(Lv1 ごとに +1%・
  最大 +5%)と「全属性増加」(Lv1 ごとに全属性 +1・最大 +10)を計算に入れます。どちらも
  テイルズID 内の全キャラに効く恒常バフなので、キャラごとに同じ Lv を入れてください。
  「ダメージ減少」「HP MP SP 増加」は記録だけ、週間 SEED / ELSO まわりは火力に効かないので
  扱いません
- **補正源に「属性」が増えました** — これまでキャラステータスの中にあった主属性の選択を、
  独立した補正源にしました。属性ごとの内訳(キャラ基礎 / 装備の属性強化 / 装備アビリティ /
  主属性)と、敵の属性値 120 / 125 に対して与ダメージが何 % 増えるかが 1 か所で読めます

### 変更

- **月石・カフス・レリックの属性値を、装備に登録したものから読むようになりました** —
  これまで属性値は「ペット・カード・ルーン・頭アビリティ・カフス」の固定 110 をまとめて
  乗せる形でしたが、月石は段(N +5 / R +10 / L +15 / G +20)で値が違い、カフスやレリックの
  属性枠も実測に幅があります。装備アビリティから実測どおりに読むようにして、固定値の
  トグルは 3 件 60(ペット +10 / モンスターカード +30 / ルーンスキル +20)に減らしました。
  **月石やカフスの属性をまだ装備に登録していないキャラは、属性値が最大 50 下がって見えます**。
  装備タブでアビリティを登録すると実際の値に戻ります

### 修正

- **盾のパッシブがキャラ画面・防御まわりにも入るようになりました** — ボリス・
  マキシミン(盾の突き → 魔攻)、ナヤトレイ・イサック・ミラ・ベンヤ・ロアミニ(バンドの敏捷 →
  斬り / 魔攻 / 魔防)は、この変換がダメージ計算タブとホームの到達判定にしか入っておらず、
  キャラ画面の装備合計・攻撃力(A)と、防御まわりの装備補正がそのぶん低く出ていました。
  装備の基本合計を出す経路を 1 つにまとめて、どの画面でも同じ値になります

- **お知らせが読みやすくなりました** — 見出しと本文を横に並べていたので、見出しの長さぶん
  本文の左端が行ごとにずれていました。見出しの下に本文を置いて左端を揃えます。
  ホームの右枠は見出しだけの 1 行にし、本文は「お知らせ」タブで読む形にしました

## [0.3.0] — 2026-09-19

### 追加

- **双剣Sub(サブ武器)に武器のアビリティが付けられます** — イソレットなど双剣を Sub に持つキャラで、
  盾の部位に「夜星の耐魔力」「下級魔法防御」といった武器側のアビリティが選べるようになりました。
  盾研磨・魔法耐性(盾)などの盾側アビリティと合わせて **2 枠**です(武器は 3 枠、普通の盾は 1 枠)。
  物理双剣は斬り系、魔法双剣は耐魔力系が候補に出ます

### 修正

- **エンチャントの MAX ボタンが押せるようになりました** — 欄の幅が足りずボタンが右の
  「装備本体補正」欄の下に隠れていて、押したつもりが隣の欄に吸われていました
- **エンチャントできないステに入力欄を出さなくなりました** — その装備でエンチャントの上限が
  無いステに「上限が未収録」と書いた入力欄が出ていて、入れられない値を入れる場所に見えていました

## [0.2.0] — 2026-09-18

### 追加

- **味方がかけるデバフを計算に入れられます** — キャラタブの補正源「スキル」に
  「敵にかけるデバフ」が増えました。同行者が敵にかけている前提なので、どのキャラでも
  オンにできます(ルシアンのそよ風、マキシミンの毒舌【暴言】、ジョシュアの弱化、
  ロアミニのカース・ペンジュラムなど 23 件)。敵の被ダメージが増える = 自分の火力が上がるので、
  オンにすると計算タブの数字がその場で動きます。上限は合計 +30% です。
  同じスキルの強さ違い(カース・ペンジュラムの通常と【シンボルオブスピリット】)は
  どちらか一方だけがオンになります

### 追加

- **ホームで装備の補正値が読めます** — ヒーローの「装備・命中」が、突き / 斬り / 魔攻 / 魔防の
  **合計**(下に `基本 +エンチャント`)+ 命中P + 回避P になりました。これまで主軸スキルの依存で
  2 本に絞っていたぶん、装備ぜんぶでいくつ乗っているかが読めませんでした。その下の
  「装備の内訳(部位ごと)」を開くと、列見出し付きの表(部位 / 装備 / 突き / 斬り / 魔攻 / 魔防)と
  合計行・そのほかの補正(物防 / 命中 / Cri / 回避 / 敏捷)が出ます。行を押すとキャラタブのその部位へ移ります
- **ホームの右にお知らせが出ます** — 最新版の変更 3 件・既知の不具合の件数を短く出し、
  更新が来ているときはその 1 行(押すとその場で更新 / 再起動)。全文は「お知らせ」タブへ
- **アナイスの熊(魔法人形)が計算に入りました** — キャラタブに「熊が撃つスキル」欄が増え、
  選ぶと計算タブに熊の鎖(1 発・合計・DPS)が本体の鎖の下に並び、その下に本体 + 熊の合計 DPS と
  討伐時間が出ます。熊の攻撃力は wiki の `STAB(熊)` 行(INT と魔攻から)で計算し、コンボは乗せず、
  攻撃間隔は中ディレイ + 0.0705 秒で見ます。ホームの到達判定とエンチャントの案内も熊込みになります。
  「いまの実力」の帯は 熊 / 本体 で切り替えられます。**保存データの形が変わります**(古い版に戻すときは
  書き出した JSON から)。鎖の左には撃つ人の絵(キャラ / 熊)が付き、「なぜこの数字?」は最後に押した
  鎖(本体 / 熊)の数字を掘り下げます。熊のときは攻撃力の掘り下げに wiki の係数行 `STAB(熊)` が出ます
- **実測タブで熊のスキルを記録できます** — スキルの候補に熊が撃つスキル(極・熊連など)が「熊」付きで
  並び、選ぶと熊の 1 発の計算値と実測の差が出ます。熊の係数は wiki が 2026/4/1 以前の情報と断って
  いるので、差が大きいときは敵の値より先に係数を疑ってください(送る本文にもその旨が入ります)
- **討伐時間が出るようになりました** — 計算タブに「討伐時間」が増え、その相手をソロで
  倒しきるまでの見込み時間が出ます(敵の HP ÷ 1 秒あたりのダメージ)。敵 42 体の HP を
  収録しました。**ソロの HP** なので、PT のときは実際より短く出ます
- **アイコンを増やしました** — キャラタブの補正源一覧と各ペインの見出しに、その補正源を表す
  ゲーム内アイテムの絵が付きました。共通スキルのペインは項目ごとに(オーグメント・極限スキル・
  アンリーシュ・パワーウェポン…)スキルの絵が付きます。装備のレリック 40 件、ライジングホリックカフス、
  アーティファクト 7 件、バフ 6 件の欠けていた絵も入りました
- **ランダムOP は部位を押したすぐ下に開きます** — 一覧の一番下ではなく、押した行の直下に
  滑って開き、もう一度押すと閉じます
- **ホームにも絵が付きました** — 「次の目標」の選択に行き先の絵(自動のときは自動で選ばれた
  行き先の絵)、「今日の強化」の 5 つのタイルにそれぞれの絵が付きました

### 変更

- **追加機能を解除していた人は、もう一度解除してください** — 追加装備の保存先と取得先を
  変えたので、前に解除して取り込んだぶんは読み込まれなくなりました。バージョン表記を
  7 回押し直せば元どおりになります(取り込むデータの中身は変わっていません)

- **スクロールバーが出ても画面の幅が動かなくなりました** — 畳みを開いた拍子に縦スクロールバーが
  出ると、ホーム・実測・お知らせ・対人の中身が少し狭くなってずれていました

- **キーボードだけで操作できるようになりました** — 上のタブ・段(「一覧から選ぶ / 一覧に無い敵」など)・
  チップは矢印キーで移動して選べ、敵や装備を選ぶ一覧は `↓` で開いて文字を打つと絞り込み、
  `Enter` で決定・`Esc` で取り消しになります。開閉する見出しは `Enter` / `Space` で開き、
  ページ内検索(`Ctrl+F`)で閉じた中の文字を探すと**その場所が自動で開きます**。
  数値の欄を `Enter` / `Esc` で閉じると、**押したその欄に戻ります**(これまでは画面の先頭に
  飛んでいました)。登録の画面の中で数値の欄を `Esc` で閉じても、画面ごと閉じてしまうことは
  なくなりました。読み上げソフトを使う場合、段・チップ・一覧に**名前が付いていなかった 31 か所**に
  名前が付きました
- **数値の欄の出方がそろいました** — 上限のある欄は、どれも「`＋` / `−` ・ 値の隣の `/上限` ・
  `MAX` ・ 上限までの進み具合のバー」が一組で付きます(素ステ・エタの意志 Lv・ランダムOP・
  アビリティの実測値などに `＋` / `−` が増えました)。**上限が無い欄**(カタログ外のエンチャント上限・
  実測ダメージ・何発中の最大)は、青枠と破線の札で「例外の手入力」だと分かるようになり、
  エンチャント上限が未収録の補正は `?` の代わりに「上限が未収録」と書かれます
- **変わった場所が光るようになりました** — これまで黙って書き換わっていた数字・文字が、
  変わった瞬間に光る / 跳ねるようになりました: ホームの「今日の強化」の面(開いたとき・
  タイルを押し替えたとき)、「最後の強化」の日付、バフセット一覧の件数、ダメージ計算の
  対象プレート(敵の名前・防御・目安)、討伐時間の HP、内訳の倍率と「ここまで」の列、
  防御 → 攻撃 に戻したときの画面ぜんぶ
- **モンスターの絵を実物に差し替えました** — アビス(ヘル)= 深淵の第2使徒、アビスEX = 第1使徒、
  アークロン地下要塞 = アンデッド兵士、最後の決戦 1 / 2 / 3 = ロカゴス / ゴイティア / 召喚の石像、
  キマイラ = メルカルト・キマイラ。レリックの聖域とアフェティリアはキシニク本人の絵になりました。
  絵の無かったアビス(ノーマル / ハード)・神鳥の塒・オルリー防衛戦にも絵が付きます
- **レリックの聖域 20段が 10〜19段と同じ行になりました** — これまで 20段だけ
  「古代レリックの聖域(キシニク)」という別行で、一覧に「20段」が出ていませんでした。
  難易度の送り(10 → 20)で選べます。**保存データの形が変わります**(選んでいた「次の目標」は
  自動で引き継がれます)
- **「行ける?」が討伐時間で分かるようになりました** — これまでの「1 発の目安ダメージ」は
  出典のない暫定値で、いまのゲームでは誰でも大きく超えてしまい判定になっていませんでした。
  **ソロで倒しきるまでの時間**に変えました: **1 分以内 = 余裕 / 3 分以内 = 通る /
  5 分以内 = ぎりぎり / 5 分を超える = 厳しい**。ホームの一覧・次の目標・強化の候補も
  同じ見方になります。時間が出せない相手(HP か動作が未収録)は、段も棒も出しません
- **バフタブの行の値が読めるようになりました** — 「ほか n」が値に重なっていたのを直し、
  行には最大のステ増分 1 件だけを出して残りは「ほか n」の中に入れました。
  火力タブでは種類が見出しで分かるので「+5%」のように % だけを出します(「その他」は
  「最終 +15%」のように短い名前付き)

- **ブラウザで使えるようになりました** — インストールせずに、リンクを開くだけで
  同じ画面をそのまま使えます。キャラの登録もダメージ計算もできます。
  データは**そのブラウザの中**に入るので、別の端末や別のブラウザからは見えません。
  ブラウザの閲覧データを消すと一緒に消えるため、**情報パネルから書き出し / 読み込み**
  (JSON ファイル 1 つ)ができるようにしました。デスクトップ版との行き来もこのファイルで行えます
- **実測タブ** — ゲームで実際に出たダメージを、条件ごと送れます。収録済みの敵なら
  このツールの計算との差がその場で出ます。**まだ収録していない敵(新しいモブ)**も、
  名前を書いて送れます。**測るときの武器をこの画面で切り替えられる**ので、キャラの登録を
  触らずに攻撃力を変えた点を何点でも記録でき、まとめて 1 通で送れます
  (2 点以上あると敵の防御力とカット率を分けて逆算できます)。
  敵の防御力・カット率は wiki でも「約」「推定値」で、集めた実測から逆算するしか
  確かめる方法がないためです
- **計算タブの「なぜこの数字?」で、切り替えがどこに効いたかを辿れます** — 称号や装備を
  切り替えると、内訳の供給源に「追加」「削除」の行が残り、閉じたカテゴリ行にも
  「+ 称号【…】」の注記が出ます。緑の ↑ を押すと、その下で変わった行だけが順に開きます。
  内訳を閉じたまま切り替えても、開けば差分が残っています

### 変更

- **オン / オフの操作を 1 つの形にそろえました** — バフ・極限スキル・キャラスキル・
  ペット会心などの「効かせる / 効かせない」は、どの画面でも同じ「行チップ」になりました。
  行全体を押すと切り替わり、オンは水色(保存される)/ ラベンダー(この画面だけ)、
  オフは白のまま値だけ薄く残ります。チェックボックスとチェック印のチップはなくなりました
- **文字を入れる欄を 1 つの形にそろえました** — キャラの呼び名・装備やオーラの登録名・
  装備 / 称号の検索・実測のメモ・問い合わせの件名と内容は、どの画面でも同じ高さ・角丸の欄になりました。
  呼び名と新しいバフセットの名前は空欄ではなく、キャラ名や「セット n」が自動で入り、押したときだけ書き換えられます。
  検索欄は左の ⌕ と右の件数で「いま何件から探しているか」が常に見え、文字数に上限のある欄は右端に「n/上限」が出ます。
  薄い文字で例を示す placeholder はなくなりました
- **「1 つ選ぶ」欄を 1 つの形にそろえました** — いつものバフ・計算タブの使うセット・主軸スキル・
  武器アビリティ・研磨の種類と乗せる先・装備の切り替え・称号・カタログ外装備の種別は、
  よく使う候補がチップで手前に並び、残りは「ほか n 件」を押すと重なって出る候補面から選べます。
  候補にはどれも選ぶのに要る値(段数・属性・増える量・件数など)が付き、「なし」「未選択」も候補の 1 行です。
  ドロップダウンはなくなりました
- **数を入れる欄を 1 つの形にそろえました** — ホームのエンチャント欄は、キャラタブの装備と同じ
  「値 /上限」のセル(底のバーで上限までの位置が見えます)と +12 / +14 / +17 / +20 / MAX になりました。
  実測タブの「何発中の最大」は − / ＋ で動かせます。上限のない「実測ダメージ(1 発)」だけは
  青い枠と「実測値 · 一時」の破線チップが付き、手で入れる欄だと分かるようにしました。
  素の数値入力欄はなくなりました
- **読み取りの面と数字の見せ方を 1 つの形にそろえました** — 防御パネル・ホームのステータス・
  対人の材料・バフの要約・実測の差は、どの画面でも同じ「ラベル + 右端の値」の行と、同じ色の
  読み取り面(水色のインセット)になりました。未収録・未登録の印は破線のバッジ 1 種類になり、
  減る値の符号はどこでも「−」(マイナス記号)で、「%」の丸め方も画面ごとに違わなくなりました。
  実測表の中ディレイ基本値は 1.00s のように 2 桁で出ます
- インストーラのアイコンをアプリのアイコンにしました(これまでは NSIS の既定アイコンでした)

### 修正

- コンボ時の「1 秒あたり」の内訳で、通常攻撃のダメージだけクリティカルでない側を出していたため、
  合計と足し算が合わないように見えていました(1 秒あたりの数値そのものは正しいままです)

## [0.1.1] — 2026-08-31

### 追加

- **更新を確認** — お知らせタブから、その場で新しい版があるか確かめられるようになりました。
  起動したときに通信できなかった場合でも、あとから自分で確認できます

## [0.1.0] — 2026-08-31

最初の公開版。

### できること

- **キャラクター管理** — 名前とキャラ種だけで登録し、素ステータス・覚醒・エタの意志・
  装備(部位別 12 スロット)・シエナのオーラ・テシスコア・称号・ランダムオプション・
  ルーン・クラウン・カード・ペット・共通スキル・マスタリーまでを編集できます
- **ダメージ計算** — キャラ・スキル・コンテンツを選ぶだけで、1 発 / 合計 / クリティカルと
  1 秒あたりの火力(DPS)を表示します
- **コンボの 1 秒あたり** — コンボは間に通常攻撃を挟むので、その通常攻撃ぶんの時間と
  ダメージも入れた 1 サイクルで火力を出します(挟む通常攻撃は選べます)
- **なぜこの数字?** — 攻撃力の内訳、相手の防御をどれだけ抜けているか、どの倍率が効いているかを
  展開して確認できます。全 30 カテゴリの計算過程もすべて追えます
- **もし〜だったら** — 装備やステータスを仮に変えたときのダメージ差を、保存せずに試せます
- **ホーム** — どのコンテンツに入れるか、何が足りないかを一覧できます
- **お知らせ** — 更新内容・これから実装するもの・既知の不具合をタブで見られます。
  中身は配信されるので、アプリを入れ替えなくても新しいお知らせが届きます
- **自動更新** — 新しいバージョンが出ると起動時に気づけて、お知らせタブのボタンひとつで
  入れ替えられます(勝手に落として当てることはしません)
- **問い合わせ** — アプリから直接ご意見・不具合報告を送れます
- **情報パネル** — 画面右上から、バージョン・データの出典・扱いを確認できます

### データの守り方

- データベースを更新のたびに自動でバックアップします。万一読み込めなくなっても、
  直前のバックアップから復元して起動します
