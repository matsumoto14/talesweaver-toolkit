# Tale Wiki を全部取り込む(計画、2026-09-02)

issue #14〜#18 の「知識」側を、wiki 丸ごとの取り込みとして具体化したもの。決まったら ADR に移す。
完成像(4 画面のワイヤー、ユーザー承認 2026-09-02): https://claude.ai/code/artifact/a5939b49-15bb-4381-9406-95364b82243a

## ゴール(ユーザー決定 2026-09-02)

wiki 3,443 ページを自前サーバー(#16 の app-api)に持ち、アプリから 4 つのことができるようにする。

1. **AI に聞ける**(#18): 「あのクエどう進める」に出典付きで答える。根拠は取り込んだ wiki だけ
2. **登録で選べる物を増やす**: 装備・神鳥・クラウン・ペット・カード等をカタログ化して自由入力をなくす(ux-guidelines)
3. **キャラに紐づけた案内**: 登録キャラの Lv・装備・進行から「次に行けるクエ / ダンジョン」を出す(ホームのブリーフィングの拡張)
4. **見やすいミラー**: 今の wiki は読みにくいので、アプリ内で整形して読める。**元ページへの出典リンクを必ず付ける**

## 先に決めること: 再配布

- talewiki にライセンス表記が無い(トップ・ヘルプ・wiki編集 を確認、2026-09-02)。#17 は「Wiki 全内容の無断再配布」を対象外と書いている
- ゴール 4(ミラー)とゴール 1(サーバーが本文を持つ)は本文の再配布にあたる。ゴール 2・3 は数値・構造の抽出なので該当しない
- 選択肢:
    - **A. 管理者に許諾を取る**。取れれば全ゴールを素直に作れる。取れるまでは B で進める
    - **B. サーバーは索引と断片だけ持つ**。全文検索・意味検索の索引と、回答に引用する見出し + 数行の断片までをサーバーに置く。本文の閲覧は元ページへリンク(ミラーは見出し・表の整形表示までに留める)
    - C. 許諾なしで全文ミラー。勧めない
- **B から始めて A を並行して打診する**のが安全。B の索引は A に昇格しても作り直しにならない

## 規模と取得(計測 2026-09-02)

| 項目 | 値 |
|---|---|
| ページ数 | 3,443(`?cmd=list`) |
| うちドメイン外(Quest / Event / NPC 好感度 / Chapter / Shop / コメント / MenuBar 等) | 約 1,500。ゴール 1・4 では対象、ゴール 2・3 では対象外 |
| wiki ソース総量 | 約 34 MB(25 ページ抽出の平均 10 KB × 3,443) |
| 取得速度 | 0.14 秒 / ページ。全件 8 分 |
| 差分検出 | `?cmd=rss&ver=2.0` に更新日時。`?cmd=list` には無い。RSS + 週 1 の全件突き合わせ(改名・削除の検出) |
| 添付(画像) | `?cmd=filelist` は一覧を返さない。ページ内の `&ref()` から辿る |

取得は既存の `.claude/skills/talewiki-fetch/scripts/fetch_page.py`(EUC-JP + NEC 拡張の復元)を土台にする。

## 層

```
talewiki ──fetch──▶ [1] 生ミラー(page, source, mtime, rev)
                          │
            ┌─────────────┼──────────────────┐
            ▼             ▼                  ▼
   [2] 構造化抽出    [3] 索引(全文 + 意味)   [4] 整形表示(PukiWiki → HTML)
   gamedata の      サーバー。#17           ゴール 4。出典リンク付き
   Rust リテラル    ゴール 1 の根拠
   ゴール 2・3
```

- [1] は派生データの正本。gamedata の数値は [2] が [1] から生成する(今の手作業スクリプトを置き換える)。domain の正本はこれまでどおり gamedata / SQLite で、索引に置き換えない(#17)
- [2] だけが今のリポの中で完結する。[1][3][4] はサーバー(#15 の境界、#16 の基盤)を要る

## 段階

各段が単独で価値を出し、次の段の土台になる順。

段階 0〜2 は issue #28、段階 3 は #17、相談画面は #18 で追う。

### 段階 0: 取込パイプライン([1] + [2] の骨格)

- `tools/gamedata/wiki/`: `fetch_all`(RSS 差分 + 全件)→ `store`(SQLite: page / source / mtime / fetched_at / status)→ `extract_*`(表 → JSON)→ `gen_rust`(`Source` + 取得日付きリテラル)→ `diff`(前回生成との差)
- 既存の `import_*.py` はこの上に載せ替える。使い捨てスクリプトを増やさない
- 受け入れ条件:
    - [ ] `python tools/gamedata/wiki/sync.py` 1 回で 3,443 ページを取り、リポ外(スクラッチまたは `%APPDATA%`)の SQLite に page / source / mtime / fetched_at / status を保存する
    - [ ] 2 回目は RSS の更新分だけ取り直し、週 1 相当の `--full` で `?cmd=list` と突き合わせて改名・削除を検出する
    - [ ] 取得失敗はページ単位で記録し、最後に成功した版を残す(全体を止めない)
    - [ ] EUC-JP + NEC 拡張(丸数字)の復元が既存 `fetch_page.py` と同じ結果になるテスト
    - [ ] 既存の 1 本(`import_skill_icons.py` か装備カタログ生成)をこの経路に載せ替え、生成物が現状と一致する
    - [ ] 生成した Rust リテラルは `Source { page, retrieved_on }` を持ち、`diff` で前回との差だけが見える

### 段階 1: 既存スキーマの穴埋め(ゴール 2 の前半、domain 変更なし)

| 対象 | 今 | 残り |
|---|---|---|
| 装備カタログ(Item/武器 32・防具 24・アクセサリ 10 ページ) | 51 件(最後のインファーナルより後だけ) | 全件(数千行)。低 Lv 帯はゴール 3 の材料にもなる |
| 称号 | 72 / 565 | 残り 493 |
| ランダム OP | 28(火力・命中・回避) | 非戦闘系 |
| 狩り場情報一覧の未消費列 | 42 体 | 必中命中P(命中P 実装済みで今は使える)・カット率B・被ダメージ倍率 |
| マスタリー | 18 キャラ | アナイス |

### 段階 2: 自由入力をカタログ化(ゴール 2 の後半)

| ページ | 行数 | 今の扱い | 変更 |
|---|---|---|---|
| 神鳥の聖物 | 124 | 数値入力 | 段階(1〜18)を選ぶ |
| クラウン | 168 | 数値入力 | ランクを選ぶ |
| PET | 159 | S スキルを選ぶ | ペットを選ぶ |
| モンスターブック/カード一覧 | 331 | 0〜70 入力 | カードを選ぶ |
| Item/消耗品/ステータス補助 | 108 | 44 件(ステータス ページ由来) | 入手先・持続・スタックを足す |
| 装備システム/アビリティ | 159 | 一部 | L-アビリティ全件 |
| 新装着アビリティ | 130 | 一部 | 全件 |
| Item/装備/セット装備 | 192 | 無い | domain に「セット効果」を足す(新概念。最後) |

### 段階 3: サーバーの索引と AI 相談(ゴール 1)

- 前提: #15(境界)と #16(app-api)。再配布の決定(上)
- [1] をサーバーに置き、[3] を作る。日本語の全文(見出し・固有名詞を強く)+ 意味検索。回答には page / 見出し / mtime を必ず返す
- #17 の受け入れ条件をそのまま使う。代表質問セット(クエストの進め方 20 問程度)を先に作る

### 段階 4: キャラに紐づけた案内(ゴール 3)

- Quest 54・Dungeon 48・Field 42・ミニゲーム 70・Lv 別一覧 35 から、入場条件(Lv / 前提クエ / 装備)と敵の Lv・攻撃種別・属性を [2] で構造化する
- `contents.rs`(今は swiki 由来)を wiki 由来に寄せる。Monster 個別 1,173 ページは湧き場所・ドロップの供給源として同じ経路で取る(計算には効かない)
- ホームの「今日の 3 件」に「次に行ける場所」を足す

### 段階 5: 整形ミラー(ゴール 4)

- PukiWiki 記法 → HTML。表・見出し・`&ref` 画像・ページ内リンク(アンカー)を扱う
- 再配布の決定が B なら、見出しと表の整形表示 + 元ページへのリンクまで。A なら全文
- 段階 3 の検索結果からここへ飛ぶ

## 段階の依存

```
段階 0 ──▶ 段階 1 ──▶ 段階 2
   │
   └──▶ 段階 3(#15, #16, 再配布の決定) ──▶ 段階 5
                 │
   段階 0 ──────┴──▶ 段階 4
```

段階 0 → 1 → 2 はサーバー無しでリポの中だけで進む。段階 3 以降はサーバー側の設計(#15)が要るので、
段階 0〜2 のあいだに #15 を書く。

## 対象外

- 添付ファイルの丸ごと保存(画像はスキル・装備アイコンなど gamedata が使う分だけ)
- コメント 360 ページ・wiki 編集 24 ページ・MenuBar 類(索引には入れるが抽出・案内には使わない)
