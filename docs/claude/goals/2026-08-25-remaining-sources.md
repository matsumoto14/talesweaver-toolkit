# Goal: 残りの補正源(ランダムOP / 称号 / 共通スキル)と上限・クリ率

状態: **計画(2026-08-25)**

## 目的

計算画面に出ている「**未実装の補正源(称号 ・ 等級 ・ ランダムOP)は中立値で計算しています**」を消す
(**段階3 で達成**。いまの注記は「キャラタブで選んだものが入る」という案内だけ)。
あわせて、前 goal(docs/claude/goals/2026-08-25-wiki-gaps.md)で別送りにした S7〜S9 を片付ける。

前 goal との違い: あちらは「**すでに画面に出ている** `未実装` / `[仮]` を実値に差し替える」だったのに対し、
こちらは**ドメインに無い補正源を新しく作る**。UI 部品(補正源ペイン)は増える。

出典は Tale Wiki(`talewiki-fetch` skill)。ページの当たりは付けてある(2026-08-25 時点):

| 対象 | ページ | 状況 |
|---|---|---|
| ランダムOP | `ランダムオプション` | **取込済み(段階2)**。火力・命中・回避に関係する 28 件 |
| 称号 | `称号/normal` / `称号/special` | **取込済み(段階3)**。主要称号(補正値の合計 15 以上)72 件 |
| 共通スキル | `Skill/共通` | **取込済み(段階4a)**。PW / SW / CA / PA / 改PA / シャープネスビジョン / オーグメント |
| 極限・ゲージスキル | `Skill/極限` | 段階4b。**3 択から 2 つ**: スコープアイ(クリダメ +20〜40%)/ フルスロットル(中ディレイ・段数)/ ワイドフォーカス(範囲) |
| 中ディレイ | `計算式まとめ#ActualDelay` / `ステータス` / `装備システム/シエナのオーラ` | `基本中ディレイ × 倍率A × 倍率B`、下限 0.3s。基本値はスキル性能一覧の「動作」列。**盾+ の RO「中ディレイ減少 1〜3%」は段階2 で記録のみとして収録済み** |
| 能力値上限 | 取込済み(docs/damage-formula.md §10) | 表はある。適用が未実装 |
| クリティカル率 | 取込済み(§9) | 式はある。対象AGI・対象のクリティカル被撃率が未収録 |

## 段階(それぞれ 1 commit 単位。上から着手する)

### 段階 1 — 「等級」の正体を反映(完了 2026-08-25)

ユーザー指摘で判明: モックの「最下〜最上」は装備の基本補正値の等級ではなく、
**装備強化 +12 以上の追加固定ダメージがレンジのどこに入るか**を指していた
(武器 +12: 140–280 / +13: 300–460 / +14: 480–660 / +15: 680–880)。

- 上書きが無いときの自動計算を**レンジ下限 → レンジ上限**に変更(装備強化再設定呪文書で振り直せるので、
  実用上の想定値は最上値)。決定記録「2026-08-25 装備強化のレンジ倍率」
- 基本補正値の等級は wiki に無い(2026-08-24 の調査どおり)ので `EquipmentPart::base` のレンジ + 上書きのまま
- 計算画面の注記からは「等級」を外す(残りは 称号・ランダムOP)

受け入れ条件: +12〜15 の自動計算がレンジ上限になる / 計算画面の注記が「称号 ・ ランダムOP」だけになる

### 段階 2 — ランダムオプション(S2)(完了 2026-08-25)

取得: `ランダムオプション`(部位ごとの節 + 転移)。決定記録「2026-08-25 ランダムオプション(段階2)」。

- **装備補正 9 値には乗らない**と判明。効き先は 3 系統だけだった
  - 依存別の攻撃力増加(盾・レリック右)→ **カテゴリP**(スキルの依存種別が一致したときだけ)
  - 攻撃ダメージ増加(盾・レリック右)→ **カテゴリX**
  - 命中率 / 回避率の増加(手・レリック左)→ **命中P・回避P への加算**
- **枠数は wiki に記載が無い**(Item ページの「スロット」列は RO 枠数ではない)。代わりに転移の説明にある
  「同じカテゴリー番号は 1 部位に 1 つまで(カテゴリー 0 は除く)」で縛った。盾のカテゴリー15 は
  依存別 6 種 + 攻撃ダメージ増加の 7 択で、**1 つしか付かない**
- `EquipmentPart::random_options` に持たせた(部位ごとなので装備側)。編集は装備の部位詳細と
  補正源「ランダムOP」ペインの両方から(同じ snippet を共有)
- 収録は火力・命中・回避に関係する 28 件だけ。効き先が計算に届かない OP(武器の条件付き追加ダメージ・
  ダメージ耐性・固定回避 / 最大回避率・中ディレイ減少)は **破線の枠 + 「記録するだけ」ラベル + 理由**で出す
- wiki の節名の対応を確定: 「サブアーム」= 盾、「サブアーム(SHOW)」= **盾+ / カフス**

受け入れ条件: 主要な部位のランダムOP を選べて、装備値かカテゴリに効く ✅ / 効き先が分からない OP は「記録するだけ」と明示 ✅ / 計算画面の注記から「ランダムOP」が消える ✅

### 段階 3 — 称号(S3)(完了 2026-08-25)

**ユーザー確定(2026-08-25)**:

- **装備枠 1 つ**。表示中の 1 件だけが効く(所持ぶんの累積ではない)
- 効き先は**装備の基本能力値への加算**(`EquipmentPart::base` と同じ列)
- **主要称号のみ収録でよい**。527 件(Normal 221 / Special 306、42 グループ)を全部入れる必要はない

wiki の称号表の列は **突き / 斬り / 物防 / 魔攻 / 魔防 / 命中 / 回避 / 敏捷 / Cri** で、
**装備補正 9 値とまったく同じ形**。`EquipmentValues` をそのまま器に使える。

- 取得: `称号/normal`(221 件)、`称号/special`(306 件)。必要なら `称号/event`
- 「主要」の基準を決めて Source に書く(補正値の合計が大きいもの / エンドゲームで実用されるもの)
- 部位は既存 12 スロットに足す形にするか、装備とは別の枠にするかを決める
  (効き先が基本能力値なので装備側が自然。ただし称号にエンチャント・強化・属性強化は無い)
- グループボーナス(N 個完成で +α)は所持状況が要るのでスコープ外。記録に留める

決定記録「2026-08-25 称号(段階3)」。

- 収録基準は**補正値 9 種の合計が 15 以上**で **72 件**。称号は 1 枠しか装備できないので、
  それ未満は選択肢として意味を持たない
- 部位(`PartSlot`)は増やさず `Equipment::title: Option<String>` にした。称号にはエンチャント・
  装備強化・属性強化・シエナのオーラ・ランダムOP が無いので、部位にすると affordance を持ちすぎる
- 補正は `Equipment::base_totals` で基本能力値に合流する
- 備考の条件付き効果(「喪失の島関連マップで追加ダメージ+20%」等)は発動条件を計算対象に
  持っていないので `note` に残すだけ

受け入れ条件: 称号を 1 つ選べて、装備の基本能力値に加算される ✅ / 計算画面の注記から「称号」が消える(= 未実装の補正源の注記そのものが消えた)✅

### 段階 4 — 共通スキル(オーグメント込み)と極限スキル 2 枠(S7)

**共通スキルが補正源として未実装**なのが根本(`StatSources` はペットS / ルーン / クラウン / 神鳥 / 常用バフ / 調整のみ)。
量が多いので **4a(共通スキル)/ 4b(極限スキル)の 2 コミット**に分ける。

#### 4a — 共通スキルと装備防御力倍率(完了 2026-08-25)

決定記録「2026-08-25 共通スキルと装備防御力倍率(段階4a)」。

- `CommonSkills` を独立した補正源として新設。パワーウェポン / ストロングウェポンを `Equipment` から移した
  (**DB v6 マイグレーション**。既存 4 キャラの移行を実機確認)
- 収録: パワーウェポン / ストロングウェポン / コートアーマー / プロテクトアーマー /
  改・プロテクトアーマー(物 9〜45% ・ 魔 6〜30%。wiki にあったので一緒に入れた)/
  シャープネスビジョン(Lv1〜10 = 5〜40%)/ オーグメント
- **オーグメントは倍率ではなく Lv 上限の前提スキル**として実装。UI の選択肢も `augment_level + 1` で絞る
- `EQUIPMENT_DEFENSE_RATE = 1.0` 固定を撤廃。`defense_profile` が倍率を引数で受け取る
- シエナのオーラの追加オプションを 2 つ追加: 「防御力増加」(0〜10%/部位 → 装備防御力倍率へ加算)、
  「中ディレイ減少」(0〜2%/部位 → 段階5 で使う。いまは記録のみ)
- シャープネスビジョンは §5 の算出基準どおり**合計ダメージ**に乗せる(1 段ごとではない)

実測: 装備防御力倍率 物199% で物理防御力 1,263 → 2,510 / SV Lv10 で 1 発 13,724 は不変・合計 13,724 → 19,213。
実機検証は docs/screenshots/108。

受け入れ条件(4a): 共通スキル(オーグメント Lv 込み)を選べる ✅ / 防御タブの防御力が倍率で動く ✅ /
リンゴの島・ベリネンルミでは装備防御力倍率が 100% 固定になる注記が出る ✅

#### 4b — 極限スキル 2 枠(次に着手)

取得: `Skill/極限`(取得 2026-08-25)。**3 択から 2 つ選ぶ**(ユーザー確定)。
エレメンタルパワー・オーバードライブは**収録しない**。

効果値は **基本 + スーパーリミット + ハイパーリミット Lv** の加算(wiki の表の列がそのまま加算になっていて、
合計が「最大値」列と一致する)。スーパーリミットは共通スキル「ハイパーアタック」、
ハイパーリミットは「エクストリームアタック」が極限状態で変化したもの。
**ハイパーリミット Lv2 以降はオーグメントの LvUp が必要**(4a と同じゲート)。

| スキル | 効果対象 | 基本 | スーパーリミット | ハイパーリミット Lv1〜6 | 最大値 | 効き先 |
|---|---|---|---|---|---|---|
| スコープアイ | クリティカルダメージ増加 | +20% | +3 | +7/+9/+11/+13/+15/+17 | +40% | **カテゴリ G**(非クリには乗らない) |
| フルスロットル | 中ディレイ | ×75% | −3 | −7/−9/−11/−13/−15/−17 | ×55% | **中ディレイ**(段階5) |
| ~ | 単体チャネリングスキル段数 | +0 | +0 | +0/+0/+0/+1/+2/+3 | +3 | **段数**(火力に直結) |
| ワイドフォーカス | スキル範囲 | +4 | +2 | +4/+6/+8/+10/+12/+14 | +20 | 火力に効かない(記録のみ) |

**単体チャネリングスキルの特定**(2026-08-25 調査): wiki「Skill#f8e303fb」の区分の凡例で
`続` = チャネリングスキル。**区分に `続` を含み、対象指定が `単体`** のものが該当し、19 キャラ 515 行を
パースして **7 件**だけだった(すべて段数 10)。

| id | 名称 |
|---|---|
| `lucian_streak` | 極・連撃 |
| `lucian_warriors_dance` | 極・無双乱舞 |
| `lucian_whirlwind_sword` | 極・旋風斬 |
| `nayatorei_mausoleum` | 極・狂猫 |
| `siberin_twin_dragon_strike` | 極・双龍撃 |
| `siberin_red_dragon_strike` | 極・紅龍連撃 |
| `isaac_demise_furious` | 極・滅神乱舞 |

受け入れ条件(4b): 極限スキル 2 枠を選べる / スコープアイでクリティカル時のダメージが増える / フルスロットルで単体チャネリングスキルの段数が増える(上記 7 件以外では増えない)

### 段階 5 — 中ディレイ(スキルの発動間隔)

**ユーザー指定(2026-08-25)**: 段階 4 のフルスロットルと同じタイミングで中ディレイの概念を入れる。
シエナのオーラの追加オプションにも出てくる。

出典: 計算式まとめ `#ActualDelay`、ステータス「中ディレイ倍率A / 倍率B」、装備システム/シエナのオーラ。

```
中ディレイ = 基本中ディレイ × (1 − 中ディレイ減少値) × (2 コンボ以上なら 0.5)   (下限 0.3s)
```

**式は wiki `#ActualDelay` の記載を正とする(2026-08-25 再取得)**。当初 goal に書いた
「倍率A × 倍率B」は wiki の現行表記と違うので上の形に直した。

- **コンボボーナス(2 コンボ以上で ×0.5)**: 減少値の上限 70% の**対象外**
- **中ディレイ減少値**: 上限 70%
  - フルスロットル −25〜−45%(= ×75% 〜 ×55%)
  - 盾+(カフス。wiki の節名は「サブアーム(SHOW)」)ランダムオプション −1〜−3%(段階2 で `cuffs-actual-delay` として収録済み。効き先を繋ぐだけ)
  - シエナのオーラ 追加オプション「中ディレイ減少」0.5% / 1% / 2%
  - キャラのパッシブ・マスタリー −5% ずつ(ボリス 剣の司祭・【一閃】、ミラ スパート −25〜0%、
    イスピン / クロエ ライバルリー、マキシミン ドタバタペア、アナイス ロキ特化、イソレット コロナゲイル 等)
- **基本中ディレイ = wiki スキル性能一覧の「動作」列**(例: 極・残影斬 1.4s、†極・横斬り 0.8s)。
  段階4b で書いた区分パーサ(`区分` / `対象指定` を取る)に「動作」列を足せば取れる
- 2025/10/29 から**全てのチャネリングスキルに中ディレイ減少が適用される**(コンボボーナス除く)
- 用途: **1 秒あたりの火力(DPS)**。いまは 1 発 / 合計しか出していないので、
  「範囲を取るか火力を取るか」の比較ができない
- CI(コンボインターバル)は「次に使うスキルの中ディレイの下限」として効く。wiki に CI 値の一覧がある
  (未極限基準)。**今回入れるかは取得後に判断**

受け入れ条件: スキルカタログに基本中ディレイが入る / 中ディレイ減少の供給源を選べる / 計算画面に中ディレイと DPS が出る / 下限 0.3s と倍率B 下限 30% で頭打ちになる

### 段階 6 — 最終能力値への上限適用(S8)

- 上限値は取込済み(docs/damage-formula.md §10。覚醒段階 + エタの意志 Lv で 1,500〜2,400)
- いまは `effective_stats` に覚醒段階が届いていないので適用していない。`stat_sources` の経路に覚醒を通す
- 上限に当たったら**捨てられた分**を出す(段階4 のダメージ上限・防御力上限と同じ出し方)

受け入れ条件: 最終能力値が上限で頭打ちになる / ステトレースに「上限で捨てられた分」が出る

### 段階 7 — クリティカル率(S9)

- 式は取込済み(docs/damage-formula.md §9)。**スキルの Cri値は S5 で収録済み**、装備クリティカル補正は段階2(前 goal)で入った
- 足りないのは **対象のAGI** と **対象のクリティカル被撃率**。`狩り場情報一覧` に値がある敵とない敵(`?`)が混在する
- `Enemy` に `agi` / `critical_taken_rate` を足し、**値がある敵だけ**クリティカル率を出す(無い敵は出さない)
- 表示先は計算タブの「クリティカル ×N」の横(期待値が読めるように)

受け入れ条件: 値のある敵でクリティカル率が出る / 値の無い敵では出さない(0% と混同させない)

## 未決定事項レジスタ(前 goal から引き継ぎ)

### ユーザー判断待ち(wiki では決まらない)

| # | 項目 | 現状 | 論点 |
|---|---|---|---|
| U1 | アイコン実画像 | 0 枚(全部 破線 `?`) | 入手元。置けば `src/assets/icons/<系統>/<id>.png` で自動反映(コード変更不要) |
| U2 | タイポ 4 段への完全統一 | 役割トークンのみ。カード見出し 11px・小ラベル 9px は据え置き | 規格どおり 19 / 12.5 / 10.5 に寄せると 3 タブすべてで密度が落ちる |
| U3 | バフ 3 状態のクリック巡回 | バッジ表示のみ。昇格は「キャラに保存」 | 巡回にすると `追加 → 常時` でチップ押下時に DB を書く |

### 別 goal(この goal でも扱わない)

| # | 項目 | 備考 |
|---|---|---|
| S6 | wiki にしか無い敵の収録 | **収録しない**(ユーザー決定 2026-08-25)。空虚の領域・次元の隙間・ヴェスティージ一般・プシーキーの虚像など |
| S10 | スキル Lv 別倍率 | wiki のスキル性能一覧は基本攻撃 SLv1 / 極 SLv10 の 1 点しか載せていない |
| S11 | アナイスの召喚獣 | 本体とは別に攻撃力・命中を持つ。スキル表も別テーブル |
| S12 | 未対応の依存 3 種 | STAB+INT / HACK+MR / INT+STAB+HACK。wiki に装備係数の基本/強化の内訳が無く、INT+STAB+HACK はステ係数が 3 項で `AttackCoefficients`(2 項)に入らない |
| S13 | 銭投げ | 攻撃力が `180 + 120 × スキルLv` で式が別枠 |
| S14 | 目安ダメージの上書き UI | **作らない**(ユーザー決定 2026-08-25)。値を直すときは gamedata を編集する |
| S15 | 称号のグループボーナス | 42 グループの「N 個完成で +α」。所持状況(527 件)の入力が要るので段階 3 のスコープ外 |
| S16 | 主要以外の称号 | 段階 3 は主要称号のみ収録(ユーザー決定 2026-08-25) |
| S17 | 極限スキルのエレメンタルパワー / オーバードライブ | **収録しない**(ユーザー決定 2026-08-25。実質 スコープアイ / ワイドフォーカス / フルスロットル の 3 択) |
| S18 | シエナのオーラの残りの追加オプション | 防御無視攻撃確率・クリティカル確率。段階4a で「防御力増加」と「中ディレイ減少」までは入れた |
| S19 | 能力解放(アンリーシュ) | 共通スキル。バフ込みステータス × +1〜+20% を 2 ステまで。**ステのどのレイヤーに乗るか**が wiki「ステータス」の一覧で未確認なので段階4a では入れていない。Lv6 以降はレインフォースが前提 |
| S20 | 装備防御力倍率の残りの供給源 | 防御力増加(Cランク・水曜日の異変)+10%、プロテクトアーマーバフ(挑戦課題)+20%、リリーローズの試薬 +10%。期間限定・イベント系なので未収録 |
| S21 | エレメンタルブレイク | 共通スキル。Lv×4% の確率で敵の属性を無視。確率発動なので未収録 |

### 採らない決定済み

- Redesign 4a / 6a / 6b / 7a(かんたんモード・一式テンプレ・充足度・精度メーター)— 12a のドリルダウンで置き換わったため
- 規格シート 3a の角丸 16px 系列 — v4 系列(12 / 9 / 6 / pill)を保つ
- Mob からの回避「率」— 敵の命中Pが取れないので出さない(回避Pだけ出す。2026-08-25 ユーザー決定)
- **等級(最下〜最上)** — 装備強化 +12 以上のレンジ振りのことだった(2026-08-25)。自動計算をレンジ上限にして解決済み。基本補正値の等級という概念は wiki に無い

## スコープ外

- 上のレジスタ U1〜U3 / S6・S10〜S14
- 装備カタログのエンドゲーム帯(Lv300/310)以外への拡張
