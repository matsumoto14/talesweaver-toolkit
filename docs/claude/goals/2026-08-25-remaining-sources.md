# Goal: 残りの補正源(等級 / ランダムOP / 称号 / 共通スキル)と上限・クリ率

状態: **計画(2026-08-25)**

## 目的

計算画面に出ている「**未実装の補正源(称号 ・ 等級 ・ ランダムOP)は中立値で計算しています**」を消す。
あわせて、前 goal(docs/claude/goals/2026-08-25-wiki-gaps.md)で別送りにした S7〜S9 を片付ける。

前 goal との違い: あちらは「**すでに画面に出ている** `未実装` / `[仮]` を実値に差し替える」だったのに対し、
こちらは**ドメインに無い補正源を新しく作る**。UI 部品(補正源ペイン)は増える。

出典は Tale Wiki(`talewiki-fetch` skill)。ページの当たりは付けてある(2026-08-25 時点):

| 対象 | ページ | 状況 |
|---|---|---|
| ランダムOP | `ランダムオプション` | 65KB。部位ごとの節(全部位共通 / 兜 / 鎧 / 武器 / サブアーム / 頭 / 体 / 手 / 脚 / レリック(右)(左))+ 転移 |
| 称号 | `称号` / `称号/normal` / `称号/special` / `称号/event` | 本体は概要のみ。実データは normal(60KB)・special(42KB) |
| 等級 | **専用ページなし** | `装備システム/MR`(マジックルーレット)が基本補正値の振り直し。等級 = レンジのどこに入るかの話の可能性が高い |
| 共通スキル | `Skill/共通` | 17KB。コートアーマー / プロテクトアーマー等 |
| 能力値上限 | 取込済み(docs/damage-formula.md §10) | 表はある。適用が未実装 |
| クリティカル率 | 取込済み(§9) | 式はある。対象AGI・対象のクリティカル被撃率が未収録 |

## 段階(それぞれ 1 commit 単位。上から着手する)

### 段階 1 — 等級(S1)

**まず「実装が要るのか」を決める。** 装備の基本能力値はすでに「wiki のレンジ(min〜max)の中央を既定にし、
実測で上書きできる」形(2026-08-24 部位別装備モデル)で持っている。等級がレンジ内のどこに入るかの
ラベルにすぎないなら、**新しいモデルは要らず表示だけの話**になる。

- 取得: `装備システム/MR`、`装備システム/インクリ`、`Item` 各ページの「等級」表記
- 決めること: 等級は (a) レンジ内の位置を示すラベル(実装不要) / (b) レンジとは別枠の倍率・加算(実装必要)
- (a) なら装備ペインに等級の目安を出すだけにして、計算画面の注記から「等級」を外す
- (b) なら `EquipmentPart` に等級を持たせ、基本能力値の決まり方を等級から導く

受け入れ条件: 等級の扱いが決定記録に残る / (a) なら計算画面の注記から「等級」が消える / (b) なら等級を選ぶと装備値が動く

### 段階 2 — ランダムオプション(S2)

- 取得: `ランダムオプション`(部位ごとの節 + 転移)
- **どのカテゴリに効くのか**を先に分類する。wiki の一覧には 装備補正 9 値に乗るもの、命中P/回避Pに乗るもの
  (「固定回避」「最大回避率」= §7 の最小回避率補正)、与ダメージ式のカテゴリに乗るもの(「物理/魔法ダメージ増加」等)が混ざっている
- 枠数は部位ごとに決まっている(register の「3 枠」は要確認)。枠数と、同じ OP が同部位で重複しない制約を wiki から確定する
- `EquipmentPart` に持たせるか、独立した補正源にするかを決める(部位ごとなら装備側が自然)

受け入れ条件: 主要な部位のランダムOP を選べて、装備値かカテゴリに効く / 効き先が分からない OP は「記録するだけ」と明示 / 計算画面の注記から「ランダムOP」が消える

### 段階 3 — 称号(S3)

- 取得: `称号/normal`、`称号/special`(必要なら `称号/event`)
- 称号は 1 つだけ装備する形か、複数枠かを確定する(register の「ランダムOP と同枠・3 枠」は要確認)
- 補正の効き先はランダムOP と同じ分類になるはず(装備値 / ステ / カテゴリ)。段階 2 の分類を再利用する
- 件数が多いので、**エンドゲームで実用される称号に絞ってよい**(絞ったことを Source に記録する)

受け入れ条件: 称号を選べて補正が効く / 計算画面の注記から「称号」が消える(= 注記そのものが消える)

### 段階 4 — 共通スキルと装備防御力倍率(S7)

- 取得: `Skill/共通`
- **共通スキルが補正源として未実装**なのが根本(`StatSources` はペットS / ルーン / クラウン / 神鳥 / 常用バフ / 調整のみ)
- コートアーマー(物+18% / 魔+12%)・プロテクトアーマー(物+36〜81% / 魔+24〜54%)等の**装備防御力倍率**を入れる
  (値は docs/damage-formula.md §6 の表に記録済み。`EQUIPMENT_DEFENSE_RATE = 1.0` 固定を外す)
- 共通スキルには最大HP/MPの固定増加やパワーウェポン/ストロングウェポン(既に装備側にある)も含まれる。
  **今回のスコープは防御力倍率まで**にして、他は記録に留める

受け入れ条件: 共通スキルを補正源として選べる / 防御タブの防御力が倍率で動く / リンゴの島・ベリネンルミでは 100% 固定になる注記が出る

### 段階 5 — 最終能力値への上限適用(S8)

- 上限値は取込済み(docs/damage-formula.md §10。覚醒段階 + エタの意志 Lv で 1,500〜2,400)
- いまは `effective_stats` に覚醒段階が届いていないので適用していない。`stat_sources` の経路に覚醒を通す
- 上限に当たったら**捨てられた分**を出す(段階4 のダメージ上限・防御力上限と同じ出し方)

受け入れ条件: 最終能力値が上限で頭打ちになる / ステトレースに「上限で捨てられた分」が出る

### 段階 6 — クリティカル率(S9)

- 式は取込済み(docs/damage-formula.md §9)。**スキルの Cri値は S5 で収録済み**、装備クリティカル補正は段階2(前 goal)で入った
- 足りないのは **対象のAGI** と **対象のクリティカル被撃率**。`狩り場情報一覧` に値がある敵とない敵(`?`)が混在する
- `Enemy` に `agi` / `critical_taken_rate` を足し、**値がある敵だけ**クリティカル率を出す(無い敵は出さない)
- 表示先は計算タブの「クリティカル ×N」の横(期待値が読めるように)

受け入れ条件: 値のある敵でクリティカル率が出る / 値の無い敵では出さない(0% と混同させない)

## 未決定事項レジスタ(前 goal から引き継ぎ)

### ユーザー判断待ち(wiki では決まらない)

| # | 項目 | 現状 | 論点 |
|---|---|---|---|
| U1 | アイコン実画像 | 0 枚(全部 破線 `?`) | 入手元。置けば `src/assets/icons/<系統>/<id>.png` で自動反映(コード変更不要) |
| U2 | タイポ 4 段への完全統一 | 役割トークンのみ。カード見出し 11px・小ラベル 9px は据え置き | 規格どおり 19 / 12.5 / 10.5 に寄せると 3 タブすべてで密度が落ちる |
| U3 | バフ 3 状態のクリック巡回 | バッジ表示のみ。昇格は「キャラに保存」 | 巡回にすると `追加 → 常時` でチップ押下時に DB を書く |

### 別 goal(この goal でも扱わない)

| # | 項目 | 備考 |
|---|---|---|
| S6 | wiki にしか無い敵の収録 | **収録しない**(ユーザー決定 2026-08-25)。空虚の領域・次元の隙間・ヴェスティージ一般・プシーキーの虚像など |
| S10 | スキル Lv 別倍率 | wiki のスキル性能一覧は基本攻撃 SLv1 / 極 SLv10 の 1 点しか載せていない |
| S11 | アナイスの召喚獣 | 本体とは別に攻撃力・命中を持つ。スキル表も別テーブル |
| S12 | 未対応の依存 3 種 | STAB+INT / HACK+MR / INT+STAB+HACK。wiki に装備係数の基本/強化の内訳が無く、INT+STAB+HACK はステ係数が 3 項で `AttackCoefficients`(2 項)に入らない |
| S13 | 銭投げ | 攻撃力が `180 + 120 × スキルLv` で式が別枠 |
| S14 | 目安ダメージの上書き UI | **作らない**(ユーザー決定 2026-08-25)。値を直すときは gamedata を編集する |

### 採らない決定済み

- Redesign 4a / 6a / 6b / 7a(かんたんモード・一式テンプレ・充足度・精度メーター)— 12a のドリルダウンで置き換わったため
- 規格シート 3a の角丸 16px 系列 — v4 系列(12 / 9 / 6 / pill)を保つ
- Mob からの回避「率」— 敵の命中Pが取れないので出さない(回避Pだけ出す。2026-08-25 ユーザー決定)

## スコープ外

- 上のレジスタ U1〜U3 / S6・S10〜S14
- 装備カタログのエンドゲーム帯(Lv300/310)以外への拡張
