# Goal: シエナのオーラ・テシスコアのモデル化

状態: **未着手(2026-08-24 方針決定)**

## 方針(ユーザー決定 2026-08-24)

キャラタブでグレー「これから」になっているシエナのオーラ・テシスコアを wiki 出典でドメインモデルに追加する。

## やること(案)

1. **wiki 調査**: シエナのオーラ(最終ダメージ+%・攻撃力+%・強化能力値加算の Lv 別値)、テシスコア(枠数・段階・強化能力値への加算)
2. **domain**: `stat_sources`(または装備系)に SienaAura(Lv)・ThesisCores(枠×段階)を追加。強化能力値への加算は装備攻撃力の「強化能力値」へ合流、シエナの最終/攻撃%はカテゴリ(New1 / L)へ配線
3. **入場条件**: `ContentRequirement::ThesisCoreTotal` を追加し、判定対象に含める(現在はモデルに無いため条件から除外している。decisions 2026-08-24 #3)
4. **UI**: キャラタブのグレー行を実ペインに昇格。強化能力値の手入力と二重計上にならない移行(現行は「強化能力値に手で含める」運用)を明示
