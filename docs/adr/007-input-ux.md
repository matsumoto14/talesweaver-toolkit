# ADR-007: 入力 UX

- ステータス: 採用
- 期間: 2026-08-21〜2026-08-26
- 元文書: goals/2026-08-21-ux-guidelines-character-screen.md、goals/2026-08-26-input-issues.md(git 履歴参照)

## 背景

旧リポジトリ(docs/legacy-twtoolkit.md)は素ステ・装備・約 60 項目のバフをセッションごとに手入力させる設計で、使うたびに同じ数値を打ち直す道具になっていた。入力欄は「自由度」ではなく「ユーザーが覚えて再入力しなければならない負債」という反省から、docs/ux-guidelines.md は「原則1: 既知の情報をユーザーに入力させない」を最上位に置き、衝突時の優先順位を「入力させない → 選択にする → 登録から分離する → 構造化する」と定めた。

2026-08-21 時点のキャラ画面はこの原則に反していた。登録フォームが素ステ/覚醒/ペット/ルーン/クラウン/聖物/バフ/調整値の 8 セクションを一度に詰め込み(分離違反)、全セクションが同じ見た目・重みで並び(構造化違反)、素ステを ± Stepper のスライダーで打たせ(選択・自由入力の区別違反)、計算画面には一時的な調整値の経路がなかった(通常操作と例外操作の区別違反)。

2026-08-26 には別の切り口で入力欄 149 件(値 71 / 段階 55 / チップ 14 / ドロップダウン 6 / 自由入力 3)を design-system.html §07 の「入力 5 形態」(自動 → 段階選択 → チップ → ステッパー → 自由入力。上から順に試し、飛ばして下に行かない)に照らして棚卸しした。数値欄 71 件すべてが形態 4(ステッパー: − / ＋ と MAX で動かし、値・上限・進捗・MAX が 1 セルに同居)を飛ばして形態 5(自由入力)で処理されており、§07 の最も強い規則に違反していた。

## 決定

- 登録は「名前 + キャラ種」のみとする。素ステ全種 1・覚醒 0/0・stat_sources 中立値はフロントで組み立てて送り、詳細はキャラ詳細画面で編集する(domain/storage の `NewCharacter` 型は変えない)
- キャラ詳細は「基本」「恒常補正」「常用バフ」「調整値」の 4 グループのアコーディオンにし、要約行を常時表示して一度に 1 グループだけ展開する。未設定のグループは「未設定(中立値で計算)」と明示する
- 保存は自動保持 + 一括保存とする。グループ切替で入力を破棄せず、どのグループの保存ボタンを押してもフォーム全体を送る
- 計算画面に「一時調整」を追加する。キャラデータは変更せず計算リクエストにだけ乗せ、トレースの補正源内訳に「一時調整」として出す
- 数値入力は `ui/StatInput.svelte` 1 部品に統一し、`Stepper.svelte` / `NumberField.svelte` は削除する
- 上限のある数値欄は形態 4(ステッパー)を既定にする: − / ＋ と MAX を常設(MAX は 1 タップで置ける)、値と上限は同じセルに同居、セル底に進捗バー、上限到達は面の色が濃くなる(色相はその欄が属する系統に従う。水色系統は金、ラベンダー系統は濃いラベンダー)
- フィールドは「表示が既定、編集は例外」とする。初期値は常に埋まっており、通常は読み取り表示、入力欄は自動値を上書きする例外操作
- 範囲(上限)は入力欄に常設する。順序があって並べられる有限選択は段階選択(`StepSelect`)に寄せ、素の `<select>` とチェックボックスは 5 形態のどれでもないため使わない

## 却下した選択肢

- 素ステの入力に ± Stepper のスライダーを使う → 廃案。範囲が広い(1–310)値にスライダー + 1 刻みボタンでは目的の値に合わせにくい
- グループ単位の部分保存(PATCH 的な差分更新) → 廃案。`update_character` が全フィールド必須の Rust 側 API と噛み合わず複雑化する。全フィールド常時保持 + 一括保存の方が「切替時に破棄しない」を追加のリセット処理なしに満たせる
- 数値欄の空欄・無効値の blur フォールバック先を `min` にする → 廃案。`min` が負の項目(調整「加算」= -999)で、全選択して打ち直そうとしただけの操作が -999 に化ける実害があった。直前の確定値へフォールバックする形に変更した
- 上限到達を示す色を全欄で金に統一する → 廃案。上限到達は「面が濃くなる」ことで示し、色相はその欄が属する系統(水色/ラベンダー等)に従う。金に統一すると系統の意味(§03)と衝突する
- アビリティ/キャラのような順序のない有限選択をチップ列(段階選択)に降ろす → 却下。規格(§14 保留 E)が明示的に否定している。段階ではないので並べても順序の意味が出ず、1 つしか選べないのに複数選べるように見える
- 監査スクリプトの「12 択以下は段階選択に降ろせる」という判定条件 → 却下。規格より粗い基準だったため、順序の有無で判定するよう直すことにした

## 経緯
- 2026-08-21: docs/ux-guidelines.md の 4 原則に照らしてキャラ画面の違反 4 件を洗い出し、登録の最小化・詳細画面のグループ化・素ステ入力の見直し・一時調整の経路を実装した。素ステは当初 `NumberField`(1–310)に統一する案だったが、同日のキャラ画面 v2 で `StatInput` 1 部品への統合に置き換わった
- 2026-08-21(同日、キャラ画面 v2): ユーザーの実使用フィードバック(入力方式が不統一、調整値の意味が伝わらない)を受け、数値入力を `ui/StatInput.svelte`(ラベル | 数値欄 | range スライダー | MAX ボタン)1 部品に統一し、`Stepper.svelte` / `NumberField.svelte` を削除した。空欄・無効値の blur フォールバックも直前の確定値に修正した
- 2026-08-25: デザインシステム適合ループで、素の `<select>` とチェックボックスが 5 形態のどれでもないことを指摘し、順序があって並べられる有限選択を `StepSelect` に寄せた。「フィールドは表示が既定・編集は例外」「範囲は常設する」を規則として確定した。上限到達色の金/紫の食い違いと、スライダーが 5 形態のどれかは「v4 の実物を見ないと決められない」保留として残した
- 2026-08-26: v4(TW Toolkit Prototype v4.dc.html)のエンチャント欄の実物を確認し、保留 2 件が決着した。v4 に `type="range"` は 0 件で、スライダーは実装の逸脱と判明し削除済みで正しいと確認された。上限到達は「面が濃くなる」ことで示し、色相は系統に従う(金/紫の食い違いは水色系統の欄についての記述として整理)ことも確定した。同じ棚卸しで、数値欄 71 件すべてが形態 4(ステッパー)を飛ばして形態 5(自由入力)で処理されている違反(G)、補正源ごとに数の意味(段階/実値/Lv)が違うのに欄が区別しない違反(A)、「OP を追加」が破線 + チップでなく段階選択の見た目になっている違反(D)、ランダム OP の欄が何の値か言えていない違反(B)を検出し、直す順序を G → A → D → B → 規格の更新 → 監査スクリプト修正の順に決めた。ドロップダウン 6 件(アビリティ/キャラ/主軸スキル)と上限の実態確認(装備補正 1,000 など)は決定待ちのまま残した
- 2026-09-01: 計算タブの「計算の材料」に**地力**(装備ではなく育てて上がるもの)のカードを 3 つ足した —
  覚醒・エタの意志 / シャープネスビジョン / ソウルリンク 5〜7。効きが大きいのにキャラタブでしか
  触れず、「盛ったらどこまで届くか」を計算タブで試せなかったため。入力形はキャラタブと同じ
  (節目の段 + 数値)にそろえ、変更は試し変更(sim)として扱う(保存しない)。差分チップも
  `awakening` / `sharpness` / `soul_link` の 3 つを新設した。
  実装中に、シャープネスビジョンの Lv 段と補助チップ(「1〜4」「未習得」)を同じ行に置くと、
  段が 10 個に増えたときチップが段の上に重なって押せなくなることを実機で検出し、行を分けた
  (§09 規則 3)。段の数で消える補助チップは、消さずに `disabled` にして幅を固定した(§09 規則 4)
- 2026-09-01: 装備ペイン先頭の「基本 / 強化」表が「壊滅的にわかりづらい」というユーザー指摘。
  原因は、灰色の小さい散文 1 塊に 4 つの別々の事実(基本に何が入る / 強化に何が入る /
  強化倍率 / テシスコアだけ入らない)を詰め、**そもそもなぜ 2 行あるのか**を一度も
  言っていなかったこと。事実を説明する対象の隣へ配り直した —
  行ヘッダに「装備 + アビリティ + 称号」「エンチャント + シエナのオーラ」を 2 段目として置き、
  テシスコアは「入りません」と文章で言わず**値の無い 3 行目**(破線 + `?`)にして、
  合流先と最大値はその下の 1 行(押すとテシスコアのペインへ移る)に出す。
  残った注記は「2 行に分かれている理由 = 攻撃力(A)で別々の係数が掛かる」だけにした。
  このとき、旧注記が **強化倍率 +20% を「強化」行の隣に置いていた**のが誤読の元だと分かった。
  `attack_power_breakdown` は `equipment_attack = base + enhanced` に掛けるので、
  倍率は**両方の合計**に掛かる。「強化能力値」と「装備攻撃力強化倍率」が
  たまたま同じ「強化」という語を共有しているだけなので、注記でそこを明示的に切り離した
- 2026-09-01(同日、続き): ユーザーから 2 点。①「計算は基本ドメインでやってるはず」— 一番良い
  地域のコア合計をフロントで `Math.max` していた(元は Workspace にあったものを summaries へ
  移しただけだが、どの地域が一番かはドメインの問い)。`ThesisCores::best_total_bonus` を足し、
  `StatPreview::thesis_core_best_total` で返して、フロントは読むだけにした。
  ②「みにくさの原因は横に伸び続けるから」— 表が 9 補正を幅いっぱいに並べ、行の名前と読みたい
  数字が画面の端と端に離れていた(§00 01 視線を動かさない)。既定を**主軸スキルが実際に使う
  攻撃補正だけ**(極・連なら突き/斬りの 2 列)にし、残りは部位詳細と同じ形のトグルで足す。
  あわせて表を幅いっぱいに引き伸ばすのをやめた(`.pane` が縦 flex なので、`width: 100%` を
  外すだけでは足りず `width: max-content` + `align-self: flex-start` が要る)。
  880px → 265px。注記も `max-width: 62ch` で折り返す
- 2026-09-01(同日、決着): 上の 2 手はどちらも「表を残したまま読みやすくする」直しで、
  ユーザーの「縦幅取りすぎ」「なんでこんなに見にくい」で行き詰まった。決め手は
  **「装備系はユーザーが強化合計(+エンチャント値)で概ね認知できる」**という前提で、
  そもそも 3 行 × 複数列の表が過剰だった。先頭の要約(表・注記・テシスコア行・列トグル)を
  丸ごと落とし、**主軸スキルが使う補正の強化合計だけをペイン見出しの右へ**出す
  (魔法斬りなら 斬り +1,058 ・ 魔攻 +1,190)。基本値は部位の行が、テシスコアは
  テシスコアのペインが持つ(「コアは無視、あくまで装備だけ」)。
  ペインを開いた瞬間に部位一覧が見えるようになり、要約ぶんの約 190px が戻った。
  見出しの右は本来「固定の説明文」の場所だが、値を置くときは `.head-value` として
  注記より 1 段強く出す。計算前(preview が無い)は 0 を並べず元の説明文に戻す
- 2026-09-01(同日、表現の確定): 装備値の見せ方をユーザーが明文化した ——
  **「合計をだしてその横にかっこでプラスエンチャント値が正しい表現」**。
  `斬り 2,974 (+1,058)` の形を `equipment.ts` の `withEnchant` / `valuesSummary` に置き、
  装備値を出す全画面(装備ペイン見出し・補正源リストの行・いまの実力バー・
  キャラとホームの部位行)をこれにそろえた。括弧はエンチャントが 0 でも出す ——
  幅が動かないうえ、`(+0)` が「まだ盛れる部位」を示す。
  それまで画面ごとに「基本合計 斬り1,916」「斬り 1,916 +1,058」「斬り530」と
  3 通りの言い方をしていて、同じ装備の話をしているように読めていなかった
- 2026-09-01: 対人タブの配置。①`Picker` の候補一覧が祖先の `overflow` で切られ、カードの枠に
  差し掛かると選べなかった。`ui/popover.ts` の `positionPopover`(fixed に置き換えてから位置決め・
  下に入らなければ上へフリップ・入り切らなければ中身をスクロール)は**まさにこの症状のために**
  作られていたのに `Picker` が乗っていなかった(使っていたのはバフタブと計算タブだけ)。
  共通部品なのでキャラ登録・ランダムOP・キャラステータス・ホーム・実測でも同時に直った。
  ②縦に伸びすぎ・右に余白が多かったので、命中率 2 行を材料より先に置き、命中P と回避Pの
  ブロックを横並びにした(1280×900 で縦スクロールなしに収まる)。**余った幅は列を広げるのではなく
  ブロックを並べて埋める** — 列を広げると視線が横に流れる(装備ペインで受けた指摘と同じ)。
  ③伸びしろはトグルにして、開いたときは素の伸びしろ(装備補正 +705)と換算後(命中P +951)を
  分けて出す
- 2026-09-01: 対人の伸びしろの言い方をダメージ計算タブにそろえた(ユーザー指摘「伸びしろの表現は
  ダメ計を参考に」)。ダメ計は「MAX で +1%」(エンチャントの伸びしろ)・「表記 ±0% / 合計 +33%」
  (もし〜だったら)と、**素の材料がいくつ増えるかではなく、それで結果がどれだけ動くか**を主役に
  している。対人の結果は命中率なので、材料ごとに `hit_rate_gain`(その材料を積んだら命中率が
  何 % 動くか。攻撃側は正・防御側は負)を domain が返す形にし、画面はそれを主役にした。
  **命中率は下限 15 / 上限 100 で挟まれる**ので、命中P を 1,514 積んでも個別では `±0%` の
  ことがある ── ダメ計の「表記 ±0%」と同じで、正直に出るのが正しい。合計は別に
  `accuracy_max_hit_rate` を返す(個別の合算にならないのは、挟み込みが非線形だから)。
  実機で内訳が「1,178 → → 命中 +1514 / 2300 P」と行の途中で折り返して読む順が壊れていたので、
  素の伸びしろと換算後を別の行に積んだ。列を広げて解こうとすると 2 ブロックの横並びが崩れて
  縦スクロールが戻るので、幅ではなく行数で解く
- 2026-09-02: 対人タブの「伸びしろ」を「**次にできること**」に作り直した(ユーザー指摘
  「DEX を上限まで、と言われても具体的に何をすればいいのか分からない」)。
    - **並びは費用の安い順で固定**(バフ → 装着アビリティ → ランダム OP → ステの固定上昇 →
      エンチャント → シエナ)。gain 降順だといちばん高いエンチャントが先頭に来て、
      「次にできること」ではなく「いちばん伸びるもの」の一覧になっていた。
      区分は Rust の `GrowthGroup` の宣言順が唯一の正で、画面は並べ替えない
    - **行は材料の名前ではなく行動**。「射手のルーンを使う 0 → 20」「手の空き枠に
      『夜星の命中』を付ける 0 → 16」「ペット S スキル(DEX)を 真Lv4 に 40 → 60」。
      Rust(`GrowthAction`)は id・名前・部位・段階だけを返し、**文言は画面が組む** ──
      Rust に `label` / `detail` の文字列を持たせていたときは、言い回しを変えるたびに
      domain のテストが動いていた
    - 装備アビリティは空き枠への装着だけでなく**同系統上位への差し替え**、ランダム OP は
      空き枠への装着(Special)と **S・真へのランク上げ**も出す。「装備の買い替えは含めない」の例外
    - **的中剣(極・的中剣)は伸びしろではなく つけ外し**。覚えられるキャラにだけ ON / OFF の
      チップを出し(既定 ON・SLv 上限)、対人タブ内の状態として `character_skills.skill_ids` を
      足し引きして `preview_versus` を叩き直す(キャラには保存しない)。
      伸びしろの行に「Lv7 まで」を並べていたのは、**つけるかどうかを選べるもの**を
      「積み上げる材料」に混ぜていたから
    - **矢印で方向を示さない**。命中率の行は「A が B に当てる」の助詞表現にした。
      `A → B` はどちらが殴る側かを読者が補っていた。**値の遷移(いま → 上限)の矢印は残す** ──
      こちらは方向ではなく変化を表している
- 2026-09-02: 「次にできること」を **3 段**(合計 → 区分 → 手)にした(ユーザー指摘
  「開くのはいいが情報が多すぎる。もう 1 階層深く」)。開くと区分(バフ / 装備アビリティ /
  ランダム OP / DEX の固定上昇 / エンチャント枠 / シエナのオーラ)だけが並び、区分を押すと
  その手が生える。区分の行の主役も「区分の手を全部打ったら命中率が何 % 動くか」で、脇に
  「N 手 ・ 命中P +X」。区分の効きは手ごとの % の足し算ではなく、Rust が区分の材料をまとめて
  差し替えて通し直した値(`GrowthGroupRooms`。命中率は下限・上限で挟まれ、DEX はステ上限で
  頭打ちになるので足し算にならない)。区分の開閉は命中P・回避Pで別、区分ごとに独立

- 2026-09-02: 対人タブを **方向ごとに 1 列**(A が B に当てる / B が A に当てる)に作り直し、
  「次にできること」は **手ごとに「試す」を入れると、その手を打った前提で率が動く**形にした
  (ユーザー指摘「あんまり直感的じゃない」「手ごとに試してみたい」。ワイヤーで 3 案を比べて決定)。
    - **決定**: 列の頭「[A] が [B] に当てる  率」の A / B がそのままキャラの Picker(上の選択カードは
      置かない。ユーザー指摘「頭が選択の場」「見りゃわかる」)。使用スキルの Picker は命中P ブロックの
      「スキルの命中」行に置く(命中P の材料だから)。列の中は上から 率 → 式 → 命中P ブロック → 回避P
      ブロック。**命中P / 回避P はブロックで、頭の行を押すと開閉**(計算タブのバフ欄と同じ感覚)。開閉は
      2 列で共有し、命中P / 回避P の段が横で揃ったまま。ブロックの中は 内訳の行 → 「全部やると
      命中率 +N% ・ いま → 上限」→ 試しの状態の行 → 手の面。手の面は残りの高さを埋め(最小 72px)、
      中でスクロールする(ユーザー指示「横並びは揃えて、中でスクロール」)。1280×840 に縦スクロールなし
    - **手の区分は 4 つ**(ユーザー指定): ステータスを伸ばす / 命中バフを使う / 装備命中補正を上げる /
      エンチャントする(回避側は 回避バフ・装備回避補正)。区分は domain の `GrowthGroup`(adr/002)。
      手は**チップ**(計算タブのバフチップと同じ見え方。デザインシステムにチェックボックスは無い)で、
      文言は**名詞**(「イルミネーション祭りのドリンク」「手: 夜星の的中剣」「エンチャント枠」)+ 命中P の
      増分 `+N`。区分の題に動詞があるので行に動詞は要らない
    - **画面で四則演算をしない**(ユーザー指摘「フロントで計算してない?」)。「全部やると +N%」の引き算も
      Rust(`accuracy_max_hit_rate_gain`)に移し、画面は Rust の値を並べるだけ
    - **試し**: 手の左の四角を入れると対人タブ内の状態(保存しない = ラベンダー)として持ち、
      的中剣チップと同じく payload に当てて `preview_versus` を叩き直す。動いた値(命中P・DEX・
      回避P・AGI・率)はラベンダー、頭に「試し N 件」と「元 → いま」(率が動いたときだけ)、
      段の頭に「試し N 件を反映中 ・ 命中P before → now ・ 全部外す」。試した手は一覧から消えず
      その場に残る(押した場所は動かない)
    - **却下**: 現状の「命中P 表 / 回避P 表 × 2 キャラ列」(率と材料が別構造で、読者が 2 表 × 2 列を
      突き合わせていた)。右ペイン案(率の行を押すと手が右に生える。幅が足りず表を狭める必要があり、
      率と材料の分離も残る)。キャラ軸案(1 人 1 枚。実装は軽いが率と材料が最も離れる)。
      「次にできること」全体の ON / OFF スイッチ(手ごとの試しで置き換え)
    - **経緯**: 実機ではキャラ差が大きく必中 / 下限に張り付いて手がほぼ全部 ±0% になるため、
      既定で見せる価値より「打ったらどうなるか」を検証できる価値のほうが大きい
- 2026-09-02: 対人タブを計算タブと突き合わせ、計算タブにあって対人に無かった型を移した(ユーザー依頼
  「計算タブと比較して改善点を調査」→「やりましょう」)。
    - **答えを主役に**: 率を 28px に(44px の主役は画面に 1 つの規格なので、2 列の主役は 1 段落とす)。
      頭の下に**答えの一文**(計算タブの「目安の 2.42 倍。火力は足りています」の役): 「必中。相手の回避P が
      あと N 上がるまで必中のまま」/「下限に張り付き。命中P があと N 上がると動き始める」/「必中まで命中P
      あと N」。N は domain の `HitRate.to_cap` / `to_leave_floor`(adr/002)。下限に張り付いた率には
      **「下限」バッジ**(式を読まないと下限だと分からなかった)
    - **手の面に率への効き**: 区分の題の右に「命中率 +N%」(Rust の `hit_rate_gain`。チップの `+N` は命中P で
      率ではない)。何を積んでも動かない列(必中 / 下限)は面ごと薄くし、頭の一言を「全部積んでも命中率は
      動かない」に。「試している手はない」の常設文言は消した(空状態の文言を常設しない §00 02。行の高さは残す)
    - **バフセット**: 計算タブの「使うセット」と同じ Picker を命中P / 回避P ブロックの材料の行に置いた
      (既定 = いつものバフ、対人タブ内だけ、保存しない)。対人向けでないバフが入っていても直せなかった
    - **畳んだときの要約**: 命中P の頭に スキル名 ・ 的中剣 ON / OFF、回避P の頭に AGI(計算タブの折りたたみ
      カードの右の要約と同じ役。畳むとスキルが見えなくなっていた)
    - **エンチャントは部位ごとに 1 手**(「エンチャント枠 +1188」1 つでは部位が分からない。計算タブは部位ごと)
    - **開閉を動かす**(ユーザー指摘「サイズが変わる時のアニメーションが不足」): 命中P / 回避P の本体に
      220ms の transition。`svelte/transition` の `slide` は height を動かすが本体は flex の子(`flex: 1`)で
      height が無視され、実機では止まったまま最後に消えた。自前の `collapse` で **max-height** を動かし、
      動いている間だけ親ブロックの flex を止める(止めないと空いた分を親が取り続け、下の段が最後に跳ぶ)
    - **命中P / 回避P は 1 枚ずつカード**(app.css の `.card` と同じ白い面 + 枠 + 角丸。ユーザー指摘
      「命中と回避の区切りが分かりづらい」)。破線 1 本の区切りだと手の面の直後に次の頭が来て、
      どこまでが命中P か読めなかった。開いているカードは頭の行の下に罫線
    - **シエナのオーラが手に出ない理由**: `siena_room` は**登録して選択中のオーラ**の残り段だけを数える。
      未登録の部位にオーラを付けるのは「買い替え」の線(adr/002)。出すかどうかはユーザー判断待ち
    - **見送り**: 試した手の「キャラに保存」(バフの手はバフセットの変更で、キャラ保存の経路と別。別 goal)、
      上部バーの「試し中」表示、根拠の帯の畳み(design-system §14 K の保留のまま)
