# 進捗状況

1 作業 1 行。決定の詳細はリンク先の ADR(adr/)と `git log`。

- 2026-09-01 [装備の「基本 / 強化」表を読めるようにする](adr/007-input-ux.md): 行に「何が入るか」を書き、テシスコアを破線 + `?` の 3 行目として置き、注記から「なぜ 2 行あるのか」(攻撃力A で別の係数)を言うようにした。強化倍率 +20% は 強化行ではなく両方の合計に掛かることも明記
- 2026-09-01 [計算の材料に地力を足す](adr/007-input-ux.md): 計算タブの「計算の材料」で、エタの意志 Lv・覚醒段階・シャープネスビジョン Lv・ソウルリンク 5〜7 を試し変更できるようにした。あわせて「次に変えるなら」の伸び率を[表記ダメージと合計ダメージの 2 本](adr/002-damage-formula-sources.md)にし、表記を動かさない候補(シャープネスビジョン・武器強化)を候補に出せるようにした
- 2026-08-29 [ソウルリンク](adr/011-soul-link.md): リンクステータス 1〜10 をキャラ単位で登録。1〜4は装備基本能力、5はクリダメG、6は最終L、7は武器追加固定ダメージへ反映し、8〜10は鎧追加HP・経験値・ELSO週上限として記録表示。旧手動Lバフは保存済みセットから除去。リンク枠・条件判定・サブ向け11〜15は持たない
- 2026-08-29 [バフセット独立化](adr/008-storage.md): storage v9 で `buff_sets` とキャラの既定セット参照を追加。旧キャラ内バフはキャラごとのセットへtransaction移行。バフタブで作成・改名・複製・削除・静的カタログ選択、計算タブでセット選択と保存されない追加/除外、キャラタブで既定セット選択ができる

- 2026-08-27 [装備登録と入力精度の刷新](adr/004-equipment-model.md): 部位別の複数登録・即時切替、基本値の自動設定、属性+9自動反映、強化等級、武器の固定ダメージ・鎧の追加HP。装着アビリティは兜・鎧・武器・盾・盾＋・頭・手・足・レリック左右に対応。効果8件、AF45件（プシーキー・エクリプス・エーテリアルの6依存×通常版/ディフェンシオを含む）、神鳥レリック左右を追加。storage v8で旧JSONを移行
- 2026-08-27 [シエナのオーラを独立登録に変更](adr/005-siena-thesis-core.md): wiki「抽出・注入」の実体に合わせ、オーラを装備登録の内包値から部位別の所持一覧 + 装着中IDへ分離。装備を切り替えてもオーラは維持され、未装着・複数登録・即時切替ができる。計算は装着中だけ、検証は登録全件。編集は装備と同じ明示クローズ式オーバーレイ
- 2026-08-27 スキルアイコン: Tale Wiki 各キャラの `Skill/<キャラ名>` ページと通常スキル303件・キャラスキル71件を表示名で機械照合し、全件同梱。3件は両カタログで同じidを共有するため `assets/icons/skills` は19キャラ371枚。取込スクリプトとPNG署名・ID網羅の検査で再現可能にした

- 2026-08-21 [最小 E2E](adr/001-domain-model.md): キャラ登録 → スキル・敵選択 → ダメージ計算 + トレースが動く。装備・属性は未実装(中立値)
- 2026-08-21 [キャラステータス補正源](adr/001-domain-model.md): ペット S スキル・ルーン・クラウン・神鳥の聖物・常用バフ・調整値を登録でき、ダメージ計算が自動反映してステトレースに寄与内訳を出す。装備値側(称号・装備アビリティ等)は未実装
- 2026-08-21 画面レイアウトの可変化: サイドバー折りたたみ・列リサイズ(docs/adr/006-ui-design-system.md)
- 2026-08-21 [キャラ画面の UX ガイドライン適用](adr/007-input-ux.md): 登録(名前+キャラ種のみ)と詳細設定を分離、ダメージ計算に一時調整の経路を追加
- 2026-08-21 [キャラ画面 v2](adr/006-ui-design-system.md): 「一覧|キャラデータ|設定」の 3 カラム、数値入力を `StatInput` に統一、調整値は加算/固定の 2 種、保存前の即時プレビュー。gamedata にプレイアブル 19 キャラ・キャラスキルバフ 9 件
- 2026-08-22 PR レビュー指摘の修正: storage の自動マイグレーション、値域検証の domain 一本化、`get_stat_limits`、調整エディタ共通化(docs/adr/008-storage.md)
- 2026-08-22 [装備攻撃力](adr/004-equipment-model.md): キャラ設定に「装備」グループ(基本/強化 × 突き/斬り/魔攻/魔防、パワーウェポン/ストロングウェポン)を追加し、攻撃力(A)の装備攻撃力が wiki 係数で効く。カテゴリ A の `[仮]` を解消。中盤以降の敵でも現実的なダメージが出る。装備防御側・武器強化の追加固定ダメージは未実装
- 2026-08-24 [v4 UI 全面刷新](adr/006-ui-design-system.md): 上部タブ+キャラレール構成・ライトテーマ(v4 モック準拠)へ移行。ホーム(コンテンツ到達一覧、`[仮]` 3 件)・計算タブの縦フロー(もし〜だったら / なぜこの数字?)・キャラタブの補正源ドリルダウン+最小登録。domain に `Content`(入場条件判定)、`preview_damage` / `evaluate_contents` コマンドを追加。部位別装備・シエナ/テシスコア等は未実装(グレーの「これから」表示)
- 2026-08-24 [部位別装備モデル](adr/004-equipment-model.md): 装備を部位別 12 スロット(アイテム選択・エンチャント・強化・武器アビリティ)のドリルダウンに刷新し、装備攻撃力・入場条件が部位集計で決まる。武器強化の追加固定ダメージ(§5)を実装。カタログはアクィルス/アビス系列+刀・太刀(他は `[仮]` カスタム入力)。旧・合計 8 値は廃止(storage v4)
- 2026-08-24 [コンテンツカタログ拡充](adr/003-enemy-content-catalog.md): 敵カタログを 3 体 → 42 体(旧リポ monsters.json 28 体+実測表「モンスター能力値リスト」14 体。wiki 狩り場情報一覧は裏取りに使用)。コンテンツは swiki「コンテンツ入場条件」から 4 エリア 59 件(下位/エタのみ/上位/その他)で、入場条件(覚醒段階・エタ Lv・装備補正)を実データ化。装備条件は使うスキルの依存(突き/斬り/魔攻/魔防/複合)で比較先が変わる方式に刷新。敵データが無いコンテンツは入場条件のみで判定。判定できない条件(ルーン Lv・共通スキル・コア等)は注記として表示。目安ダメージは `[仮]`
- 2026-08-24 [シエナのオーラ・テシスコア](adr/005-siena-thesis-core.md): キャラタブのグレー行 2 件を実ペインに昇格。シエナのオーラは部位ごと(8 部位)に増幅段階・能力値・追加オプションを持ち、武器/盾は強化能力値・その他はステ最終固定値・攻撃力増加は New1 に入る。テシスコアは地域ごとに 6 枠(タイプ 8 種 × 進化 0〜4 × 強化 0〜4)を持ち、火力タイプの補正は対象コンテンツの地域のときだけ強化能力値に合流、補助タイプは装備値(`SupportValues`)として保持するだけ、セット効果は地域ごとに発動して重複し K/L に乗る。入場条件の「コア N」を `ThesisCoreTotal` として実判定化(注記から昇格)。計算コマンドは敵 id ではなくコンテンツ id を受け取る形に変更
- 2026-08-25 [デザインモック整合 段階1: 攻撃力の可視化](adr/006-ui-design-system.md): キャラに主軸スキル(`main_skill_id`、storage v5)を持たせ、キャラ画面に攻撃力(A)と内訳(ステ/装備基本/装備強化/強化倍率)、部位ごとの寄与(外すと減る量)を常設。算出はダメージ計算と同じ `attack_power_breakdown` を共有。テシスコアは地域依存のため地域なしの値であることを明記
- 2026-08-25 [デザインモック整合 段階2: 装備ペインの密度](adr/006-ui-design-system.md): 武器アビリティを系統(`EquipmentAbilityFamily`)ごとの Select 4 行にして同系統の重複を UI と storage の両方で排除。エンチャントに上限ゲージ(`値 / 上限` + 満)、基本とエンチャントを左右 2 列に、装備値の合計を表に、部位行にアビリティ数を追加。補正源の並びを 12a 順にしてヘッダに「未設定 N 件」。パワーウェポン/ストロングウェポン Lv6 を新規登録キャラの既定にして 1 行へ畳んだ
- 2026-08-25 [デザインモック整合 段階3: デザイントークン](adr/006-ui-design-system.md): 角丸を 4 段(`--r-window`/`--r-panel`/`--r-inset`/`--r-pill`)に固定して直書きを全廃。タイポは 4 つの役割トークン(`--t-result`/`--t-heading`/`--t-body`/`--t-label`)を入れ、役割が一致する箇所だけ置換(カード見出し 11px・小ラベル 9px は v4 の密度を保つため据え置き)。インセット面 `--surface-inset` を追加して表・読み取り専用の数値に適用、ラベンダーを `--sim` としてトークン化
- 2026-08-25 [デザインモック整合 段階4: アイコン規格](adr/006-ui-design-system.md): `ui/Icon.svelte` を追加し、4 系統(キャラ/Mob/スキル/バフ)を枠色と角丸で識別・サイズ 4 段(20/28/40/64)に固定。画像は `src/assets/icons/<系統>/<id>.png` を gamedata の id から機械的に解決(手動マッピングなし)。解決できない id は破線 + `?` + console warn 1 行、対象が無い枠は縞のまま。実画像 0 枚でもレイアウトが崩れないことを実機で確認
- 2026-08-25 [デザインモック整合 段階5: 上限ロスと防御側](adr/006-ui-design-system.md): 上限で捨てられた分を `生値 → 上限値 / N は無効` で表示(`CategoryTrace.raw − value`)。`domain::defense_profile` を追加し、防御力(物理/魔法/複合)・カット率 J `r = 1 − a/(a+80)`・特殊回避を「攻撃 / 防御の同列タブ」で出す。装備物防・通常回避・最終被弾率は `Option::None` にして破線「未実装」で 0 と区別。未実装の補正源が中立値で計算されている旨も計算画面に明記
- 2026-08-25 [デザインモック整合 段階6: 小粒 3 件](adr/006-ui-design-system.md): スキルピッカーを合計ダメージ降順に。`ContentSeries` を追加してレリックの聖域 10 行を 1 行 +「◀ 難易度 N / 19 ▶」に畳む(系列は id の接頭辞 + 末尾の数値で機械的に解決)。バフチップを 3 状態表示(常時 = キャラに保存済み / 追加 = 試し変更 / 無印 = 使わない)にし、常時への昇格は既存の保存導線に寄せた
- 2026-08-25 [wiki 未取込データの取込 段階1: 命中・回避](adr/002-damage-formula-sources.md): wiki 計算式まとめ `#HitRate`/`#AccuracyPoint`/`#EvasionPoint`/`#HitRateCap` を docs/damage-formula.md §7 に取り込み、回避P(物理/魔法/複合)・通常回避の上限 85%・上限回避時の最終被弾率を実値化。防御タブの破線「未実装」は 3 件 → 1 件(装備物防のみ)。コンテンツ別の通常回避「率」は wiki 狩り場情報一覧の「上限回避P」が全行未記載のため未達(`domain::normal_evasion` は汎用形で実装済み、段階5 で接続)
- 2026-08-25 [wiki 未取込データの取込 段階2: 装備の防御側](adr/002-damage-formula-sources.md): `EquipmentValues` を 4 値 → 9 値(突き/斬り/物防/魔攻/魔防/命中/Cri/回避/敏捷)にして `SupportValues` を廃止。カタログ 20 件を wiki Item ページの 9 列 + 「上限」行で書き直し、装備物防が防御力・カット率に、装備回避率/敏捷度が回避Pに入るようにした。防御タブの破線「未実装」が 0 件に。エンチャント編集が 9 行、部位行の要約は「値が大きい上位 2 種」。装備防御力倍率(共通スキル依存)と `EQUIPMENT_VALUE_MAX` の `[仮]` は理由付きで残す
- 2026-08-25 [wiki 未取込データの取込 段階3: 属性システム](adr/002-damage-formula-sources.md): `Element`(8 種)/`ElementValues` を domain に追加し、キャラの基礎属性値(wiki 各キャラ表、15 キャラ)+ 装備の属性強化(部位ごとに 1 属性・0〜9)で属性値を出せるようにした。`Skill::element` を追加し、wiki から読み取れないスキルは `None`(属性差ボーナスなし)。`Enemy::element_threshold` は「敵属性値」と確定して `[仮]` を解除。カテゴリI が実機で 1.0 以外(+6%)になることを確認。属性値の供給源が足りない件(最大 100 < 敵 120)は U6 として未解決記録
- 2026-08-25 [wiki 未取込データの取込 段階4: 覚醒・エタの意志の上限](adr/002-damage-formula-sources.md): 覚醒倍率を wiki(Quest/覚醒クエスト・エタの意志)で裏取りして `[仮]` を解消(旧リポ値と Lv0〜80 が完全一致)、表を Lv100 まで拡張。ダメージ上限(1 段ごと)と防御力の上限を実装し、上限で捨てられた分を計算画面・防御タブに表示。最終能力値の上限 2,400 の根拠(エタ Lv100 の最大ステータス開放)が確定。能力値そのものへの上限適用は未実装
- 2026-08-25 [wiki 未取込データの取込 段階5: 敵データの出典整理](adr/002-damage-formula-sources.md): 敵 42 体を wiki 再取得版と再照合して全件一致を確認し、`ENEMIES_SOURCE` を wiki 先頭・2026-08-25 に更新。目安ダメージは wiki に無い値と確定して `[仮]` を「コミュニティ知識・実測」に読み替え(UI の「仮値」表記も修正)。wiki にあって未消費の列(必中命中P・敵AGI・クリティカル被撃率・被ダメージ倍率・カット率B)を docs/damage-formula.md §10 に一覧化
- 2026-08-25 [wiki 未取込データの取込 段階6: クリティカル](adr/002-damage-formula-sources.md): 非クリティカル時に `{F×G}` ごと 1.0 とする `[仮]` を解消(wiki の [G] クリティカルダメージ増加の供給源がすべてクリティカル時限定)。複合カット率の `[仮]`(U4)も wiki ステータス `#DamageReductionPlayer` の記載で解消。クリティカル率の式は docs/damage-formula.md §9 に記録して未実装(対象AGI・被撃率・スキルクリティカル率が揃わない)
- 2026-08-25 [wiki 未取込データの取込 段階7: ボリスのスキル](adr/002-damage-formula-sources.md): ボリス 5 件を wiki「スキル性能一覧」から取り込み(倍率・段数・Cri倍は旧リポ値と全件一致で `[仮]` 解消)。属性 / スキル命中 / Cri値 / SLv を `Skill` に追加し、段階3 で読み取れなかった 2 件の属性が確定。命中P を実装(DEX + 装備命中率補正 + スキル命中 ± 依存補正)して計算タブのスキル行に表示。他 18 キャラのスキルは未取込
- 2026-08-25 [wiki 未取込データの取込 レビュー反映](adr/002-damage-formula-sources.md): 属性値の供給源をユーザー確定情報で補完(装備 10 部位×9 = 90 + ペット10 / カード30 / ルーン20 / 頭アビ20 / カフスアビ30)。補正源に「属性」ペインを新設し、キャラ基礎 / 装備 / 供給源 / 合計の内訳表を出す。合計 210 で敵属性値 120 に対し属性差 +90 = カテゴリI 上限に届く(U6 解消)。Mob からの回避「率」は算出不能と確定して打ち切り、防御タブは回避P + 特殊回避のみに
- 2026-08-25 [全キャラのスキル取込(S5)](adr/002-damage-formula-sources.md): wiki 各キャラの「スキル性能一覧」をパーサで取り込み、スキルカタログを 5 件 → **19 キャラ 303 件**に。倍率 / 段数 / Cri倍 / 属性 / スキル命中 / Cri値 / SLv を収録(ボリスの 5 件は旧リポ値と完全一致)。id は wiki アンカー由来に変更(旧 `boris_goku_*` は失効)。スキル命中・Cri値は wiki が `-` の 28 件を `None` に。除外は 銭投げ・変種行・未対応の依存 3 種
- 2026-08-25 [装備強化のレンジ倍率を上限に](adr/002-damage-formula-sources.md): モックの「等級(最下〜最上)」は装備強化 +12 以上の追加固定ダメージのレンジ振りを指していた(ユーザー指摘)。上書きが無いときの自動計算をレンジ下限 → **上限**に変更(再設定呪文書で振り直せるので実用上の想定値は最上値)。検証ボリスの †アビスシミター+12 で追加固定ダメージ 311,220 → 622,440。計算画面の注記も「称号 ・ ランダムOP」だけになった
- 2026-08-25 [ランダムオプション(段階2)](adr/002-damage-formula-sources.md): wiki「ランダムオプション」を取り込み、**装備補正 9 値には乗らない**ことを確定(効き先は カテゴリP 特定依存ダメージ増加 / カテゴリX 攻撃ダメージ / 命中P・回避P の 3 系統)。枠数は wiki に記載が無いので「同じカテゴリー番号は 1 部位に 1 つまで(カテゴリー 0 は除く)」で縛った(盾のカテゴリー15 は依存別 6 種 + 攻撃ダメージ増加の 7 択で 1 つだけ)。収録は火力・命中・回避に関係する 28 件で、効き先が計算に届かない OP(武器の条件付き追加ダメージ・ダメージ耐性・固定回避 / 最大回避率・中ディレイ減少)は破線枠 +「記録するだけ」+ 理由で表示。補正源に「ランダムOP」ペインを新設し、装備の部位詳細からも同じ編集ができる。実測: 盾「物理複合攻撃力が増加 S・真 28%」で P が ×1.28、手「命中率が増加 S・真 20」で命中P 131 → 151。計算画面の注記は「未実装の補正源(**称号**)」だけになった
- 2026-08-25 [称号(段階3)](adr/002-damage-formula-sources.md): wiki「称号/normal」「称号/special」全 565 件をパーサで取り込み、**主要称号 72 件**(補正値 9 種の合計 15 以上)をカタログ化。称号は**装備枠 1 つで表示中の 1 件だけ**が効き(所持ぶんの累積ではない)、効き先は**装備の基本能力値**(wiki の表の列が装備補正 9 値と同じ顔ぶれ)。`PartSlot` は増やさず `Equipment::title` にした(称号にはエンチャント・強化・属性強化・シエナ・ランダムOP が無いため)。補正源に「称号」ペインを新設(検索付き・補正値降順)。備考の条件付き効果(「喪失の島関連マップで追加ダメージ+20%」等)とグループボーナスは未実装で `note` に残すだけ。あわせて表示用 `equipmentBaseTotal` が武器アビリティと称号を落としていたバグを修正(Rust の `base_totals` と顔ぶれを揃えた)。実測: エクリプス(全 9 値 +40)で装備値の基本行が 9 列すべて +40、攻撃力(A)が 7,590 → 8,975。計算画面から「未実装の補正源」の注記が消えた
- 2026-08-25 [共通スキルと装備防御力倍率(段階4a)](adr/002-damage-formula-sources.md): 共通スキル(wiki Skill/共通)を独立した補正源 `CommonSkills` として新設。パワーウェポン・ストロングウェポンを `Equipment` から移し(**DB v6 マイグレーション**)、コートアーマー / プロテクトアーマー / 改・プロテクトアーマー / シャープネスビジョン / オーグメントを追加。`EQUIPMENT_DEFENSE_RATE = 1.0` 固定を外して装備防御力倍率が動くようにし、シエナのオーラの追加オプション「防御力増加」(0〜10%/部位)と「中ディレイ減少」(記録のみ)も収録。オーグメントは倍率ではなく **Lv 上限の前提スキル**として扱う(Lv2 以降に必要)。シャープネスビジョンは §5 の「新-割合」どおり**合計ダメージ**に乗せる(1 発ごとではない)。実測: 装備防御力倍率 物199% で物理防御力 1,263 → 2,510、SV Lv10 で合計 13,724 → 19,213(1 発は不変)
- 2026-08-25 [極限スキル 2 枠(段階4b)](adr/002-damage-formula-sources.md): wiki「Skill/極限」の 3 種(スコープアイ / フルスロットル / ワイドフォーカス)を **2 枠**で選べるようにした。効果値は wiki の表どおり **基本 + スーパーリミット + ハイパーリミット Lv** の加算で、ハイパーリミットも 4a と同じオーグメントゲートに従う。スコープアイは**カテゴリG**(非クリには乗らない)、フルスロットルは**単体チャネリングスキルの段数 +3**、ワイドフォーカスは火力に効かないと明示。単体チャネリングの特定は wiki「Skill#f8e303fb」の区分の凡例(`続` = チャネリング)+ 対象指定 `単体` で、19 キャラ 515 行から **7 件**(いずれも段数 10)。実測: 極・連撃でクリティカル合計 49,350 → 91,000(G ×1.40)、段数 10 → 13。非チャネリングの 極・殺 では段数もダメージも変わらないことを確認。あわせてオーグメント Lv を下げたときに従属 Lv(SW / PA / ハイパーリミット)が取り残されて保存だけ失敗する UX 不具合を修正
- 2026-08-25 [中ディレイと DPS(段階5)](adr/002-damage-formula-sources.md): wiki「計算式まとめ #ActualDelay」の式(`基本中ディレイ × (1 − 減少値) × (2 コンボ以上なら 0.5)`、下限 0.3s)を実装し、**1 秒あたりの火力(DPS)** を計算タブに出せるようにした。基本中ディレイは各キャラのスキル性能一覧「動作」列を **303 件すべて**取り込み(`A/B` 表記は基本値、`(固定)` 2 件は減少が効かない、秒として読めない 1 件は DPS を出さない)。中ディレイ減少値(上限 70%)の供給源 4 つ — フルスロットル / カフスのランダムOP(記録のみ → 実装)/ シエナのオーラ / キャラのパッシブ・マスタリー 9 件(補正源ペイン「中ディレイ減少」を新設)— を繋いだ。コンボボーナス(×0.5)は倍率A なので上限の対象外。あわせて段階4a で入力欄が無かったシエナのオーラの「防御力増加」「中ディレイ減少」に入力欄を追加。実測: フルスロットル(ハイパーリミット Lv6 −42%)+ 剣の司祭 −5% + マスタリー【一閃】 −5% で †極・横斬り の中ディレイ 0.80s → 0.38s、1 秒あたり 12,250 → 25,521・113
- 2026-08-25 [最終能力値への上限適用(段階6)](adr/002-damage-formula-sources.md): 覚醒段階 + エタの意志 Lv で決まる**最終能力値の上限**(1,500〜2,400)を適用するようにした。取込済みだった `AwakeningCaps::max_stat` を `effective_stat` の引数として通し(ダメージ上限と同じ形)、上限で捨てられた分を `StatTrace::capped_loss` に残す。計算タブの詳細トレースに「上限 / 上限で捨てた分」の 2 列、キャラ画面の最終能力値に「上限」バッジを追加。覚醒が届いていなかったキャラ画面の即時プレビューにも `awakening` を通した。調整の「固定(pin)」は実測値を入れるための例外操作なので上限のあとに適用する(上限で削らない)。実測: エタの意志 Lv0(上限 1,500)+ クラウン DEX300 + 調整 DEX+999 で 最終能力値 1,500 で頭打ち
- 2026-08-25 [クリティカル率(段階7)](adr/002-damage-formula-sources.md): wiki「計算式まとめ #CriticalChance」の式を実装し、計算タブの「クリティカル ×N」の横に**発生率**を出せるようにした。`Enemy` に `agi` / `critical_taken_rate` を追加し、狩り場情報一覧から敵AGI 19 体・クリティカル被撃率 11 体を収録(残りは wiki が `?`)。被撃率は −250〜−930% と式の中で支配的なので、**両方そろっている敵でだけ**率を出す(AGI だけの敵・スキルの Cri値が未記載のスキルでは出さず、理由を表示)。クリティカル率増加(上限 +100%)は値が確定している 3 件(極のルーン +20 / 設計者の研究室 +30 / 致命打 +100)とペット会心 ×1.1 を補正源ペイン「クリティカル率」で選べるようにした。敵の値は wiki 側が「約」「推定値」と書くコミュニティ実測なので `[仮]`。実測: エクリプスボス(敵AGI 1,420 / 被撃率 −350%)で「クリティカル ×2 ・ 発生 0.0%」+ 内訳、トゥタトゥール(wiki に対応行なし)では率を出さず理由を表示・113
- 2026-08-25 [DPS を実測のスキル回数表で出す](adr/002-damage-formula-sources.md): wiki `#ActualDelay` 自身が「発動頻度が急激に変化する場合がある(丸め処理・通信頻度・入力間隔)」と注記しているとおり、実際の回数は `60 / 中ディレイ` にならない(実測は式より 3〜14% 少ない)。ユーザー提供の計測表「260702 테섭 허수아비 기준」(総中ディレイ減少 48〜70% × 基本中ディレイ 0.8〜1.6s の **60 秒あたりスキル回数**)を収録し、`DPS = 合計 × 回数 ÷ 60` に切り替えた。格子の内側は 2 次元の線形補間、外側(総減少 48% 未満・0.8s 未満 / 1.6s 超・コンボボーナス時・「(固定)」の中ディレイ)は式にフォールバックして画面に明示する。表の `보리스 26% (실적용22%)` が **48+26=74% → 上限 70% で頭打ち**を示していて、実装の減少値上限と供給源モデル(シエナ 2%×8 部位 + パッシブ 5%)の裏取りになった。CI(コンボインターバル)は考慮しない(ユーザー決定。コンボによる DPS はサポート外)
- 2026-08-25 [アンリーシュ(能力解放)](adr/002-damage-formula-sources.md): S19 の未解決点だった「ステのどのレイヤーに乗るか」が wiki「ステータス」の**能力値倍率B** の表で確定したので実装。共通スキルで**選んだステを 2 つまで**解放し、Lv1〜10 = +1/+2/+3/+4/+5/+8/+11/+14/+17/+20%(既存の `[基本能力値 × 倍率B]` 層に加算)。**Lv6 以降はレインフォース(Lv1〜5)が前提**でオーグメントと同じゲート挙動(前提スキルを下げると従属 Lv も下がる)。同じステは 2 枠に入れられない。ユーザーの実測(基本能力値 506 で 990/995/1000/1005/1010/1025/1040/1055/1071/1086)と **1 の位まで一致**し、Lv9 だけ差が +16 になるのも `[506×0.17]=[86.02]=86` の切捨てで説明できた。実測を再現するテストを `common_skill.rs` に置いた。ユーザーの実環境の内訳(AGI 1086 = 基本 506 + アンリーシュ 101 + 最終固定値 479)でも裏取り。あわせて、キャラ画面の即時プレビューが共通スキルの**ネストした値**の変更で再計算されない不具合(`$effect` の浅いコピー)を修正
- 2026-08-25 [モンスターカード(カード装着)](adr/002-damage-formula-sources.md): 補正源の一覧に「これから」で残っていたモンスターカードを実装(ステごと 0〜70、**固定値層**)。wiki「ステータス」の固定値増加の表にある「カード装着 +0〜70 / 装着カードのステータス」が出典で、ユーザーの実環境で +70 が実際に乗っていた。これで実測の内訳が**残差ゼロ**で再現できるようになった: `固定値層 150(ペット真Lv2 40 + ルーン 20 + カード 70 + クラブSエフェクト 20)` → `基本能力値 [(310+150)×1.1] = 506` → `最終 506 + [506×20%] + 最終固定値 479(聖物350 + クラウン100 + シエナ29) = 1086`
- 2026-08-25 [シエナのオーラのクリティカル確率(S18)](adr/002-damage-formula-sources.md): 段階7 でクリティカル率を実装して効き先が確定したので、シエナのオーラの追加オプション「クリティカル確率」(1 部位 0〜10%)を収録。wiki の備考どおり `#CriticalChance` の **AGI 由来の項に掛かる乗数**(ペット会心と同じ位置)で、`× (1 + Σ%)`。同じ S18 の「防御無視攻撃確率」は **x% の確率で防御力 15% 無視**の確率発動で、wiki 自身が「他の強打効果と併用すると加算される不具合(?)」「複数装着しても増幅しない」と書いていて期待値も確定しないため、エレメンタルブレイクと同じ理由で**収録しない**と決めた
- 2026-08-25 [UI をデザインシステムに適合させる](adr/006-ui-design-system.md): docs/design-system.html を正として周回中(12 周目)。11 周目で一度は終了条件を満たしたが、**「押すと状態が変わる要素」を §09 規則 1 の監査経路に入れていなかった**ため、バフを押すと押したチップが 69px 動く違反が残っていた(ユーザー指摘)。機械監査 `tools/design-audit/run.py`(10 ルール)と実機監査 `tools/design-audit/live/`(8 本)を用意し、以後の周で毎回走らせる形にした。主な着地: **色をすべてトークン経由に**(状態の 6 系統を `src/ui/states.ts` に集約、内訳の色 `--flow-*` を新設)、**§10「動きと変化」を実装**(実質未実装だった。数値の跳ね・ペインの出現・ポップオーバー・バッジ・開閉 + reduced-motion)、**装備のドリルダウンを 3 ペイン化**(置き換えをやめて右に増やす)、**入力を 5 形態に寄せる**(素の `<select>` → 段階選択、チェックボックス → チップ、フィールドは「表示が既定・編集は例外」)、**§14 の決定 6 件をすべて実装**(鎖 / 効いていない分の棚卸し / 補正源の並び = お気に入り + ドラッグ / 相手依存の値 / 収録度バッジ / 試し変更 3 件)。規格側も 6 箇所直した(トークン名の併記・内訳の色の節・コストのタグ・実寸スケールに 10・斜線の但し書き・§14 の整理)。**保留 6 件(A〜F)は v4 の実物を見れば決まる**ものとして §14 に登録(132〜134 が最終の 3 タブ)
- 2026-08-26 シエナのオーラの入力刷新・称号のダメージ増加(adr/005-siena-thesis-core.md): オーラは「スロットを 1 個ずつ選んで積む」形に、称号/event の「ダメージ n% 増加」をカテゴリX に取り込み
- 2026-08-27 [マスタリー](adr/002-damage-formula-sources.md): wiki 各キャラの「マスタリースキル」表から 18 キャラ 243 件を収録。段(M1〜M4、リーチェのみ 15 段)ごとに 3 択で 1 つ。効き先がばらばら(中ディレイ / カテゴリX / ステ / 未収録)なので 1 カタログにまとめ、計算に入るのは無条件で言い切っているものだけ。アイコン 243 枚も wiki から取込
- 2026-08-27 [与ダメージ式のカテゴリに供給源を配線](adr/002-damage-formula-sources.md): 効き先が「ステ増加」専用の器に縛られていて、**同じバフが 2 つのカテゴリに効いていても片方しか入らなかった**問題を解いた。`SkillEffect::Damage { category, percent }` に一般化してバフ・キャラスキル・マスタリー・装備アビリティで共有し、上限はカテゴリ側が持つ形に。**キャラスキルの器を 1 本に統合**し、カテゴリXを X1〜X6 に分割。装備アイテムの効果も含めて**X1〜X6すべてに供給源が入った**。さらに新装着アビリティのカテゴリー4へランダム追加2枠を実装し、固定ダメージ `W`、割合 `X3`、攻撃補正・命中を計算へ接続した。HP/MP回復は記録のみ。残りは手/兜/鎧アビリティの部位対応、232〜233
- 2026-08-27 [装備登録UX](adr/004-equipment-model.md): ボリスの極・連 / 極・残影斬 / 極・氷撃斬から刀 / 太刀 / 大剣へ候補を直接絞り、大剣2件と公式アイコンを追加(装備カタログ51件)。装備補正は S/H/I/M を先頭4枠、残り5補正を折り畳み、`合計(+エンチャント)` とアビ加算を同じ行に表示。武器アビリティ本体もランダム追加もプルダウンを使わず、固定3段の候補チップと選択済み追加行で完結させ、反映要約も常設した。登録画面は背景や候補選択で閉じず、スクロール中も見える「閉じる ×」か Escape だけで閉じる、242〜243
- 2026-08-30 [表記ダメージと追加ダメージの分離・カテゴリE2 の % 換算](adr/002-damage-formula-sources.md): 実測との乖離(見つめる悲しみで 21M / 実際 7〜10M)を 2 点直した。武器強化の追加固定ダメージを `per_hit` から分離して `weapon_added_per_hit` にし(ゲームは表記ダメージと別枠で出す。1 段スキルだと追加分がまるごと表記に乗って見えていた)、ダメージ上限は与ダメージにだけ掛かるようにした。到達判定もこの表記ダメージ基準。カテゴリE2「スキル倍率増加(固定値)」は wiki の供給源がすべて % 表記なので 1/100 する — 兜「E-スキル攻撃力増加 +10」を実数で足していてスキル倍率が 4.62 → 14.62 になり火力が 3 倍以上に膨らんでいた(旧リポ twtoolkit の Excel v4.00 実装とも一致を確認)。修正後の閃花月光斬 = 見つめる悲しみH で 5.68M / N で 10.7M
- 2026-08-30 [ゲーム計算をフロントから剥がして domain へ戻す](adr/001-domain-model.md): 係数の直書き(共通スキルの倍率・コンボ閾値・中ディレイの上限/下限・防御/カット率/回避P/属性差の係数)を `StatLimits` 経由に、domain と同じ集計の再実装(装備値の合計・属性値・ランダムOP / シエナの効き先別合計・称号 / テシスコアの合計・マスタリーによる効果差し替え・中ディレイの供給源合算・クリ率の内訳・聖物の段階換算)を `StatPreview` / 新コマンド経由に置き換えた。`limits.svelte.ts` の FALLBACK と `state.svelte.ts` のシエナ既定値(domain 定数の複製)を撤廃し、`main.ts` が上限を取り切ってから `App.svelte` を動的 import する。副産物として「強化」欄のシエナ落ちとクリ率ペインの装備クリ補正落ちが解消。コンボは「ダメージ +15% は 3 コンボ以上 / 中ディレイ減少は 2 コンボ以上」と別物として表示するようにした
- 2026-08-30 [キャラタブの下部シートを 1 行の結果バーに](adr/006-ui-design-system.md): 「いまの実力」シートが 285px を占めながらカード 7 枚のうち 5 枚しか出せず、220px の内部スクロールと粒度の混在で読めなくなっていた。**補正源が「いくら効いているか」は各補正源ペインの先頭へ返し**(ランダムOP / 共通スキル / テシスコア、装備値の全部位合計は装備ペインの部位一覧先頭)、シートは結果だけの帯にした。結果 2 枚に絞った時点で「最終能力値」がトリガ行と同じ内容になったので開閉ごと撤去し、**36px の 1 行バー**に。中身は火力の材料 — 主軸スキルの依存種別で絞った装備値(基本 +強化)と、ダメージと取り違えないよう**ラベルを値の前に置いた**「攻撃力(A)」、右端は「ダメージを見る ›」でダメージ計算タブへ。7 ステの一覧・左カラムの「いまの攻撃力」カード(シートと重複)・「このキャラで通るのは」(左レールのバッジと重複)は削除し、「このキャラを削除」はツールバーへ。行サブタイトルとペインの両方から使う計算は `pages/chars/summaries.ts` に純関数として集約。空いた 250px で補正源リストが 5 件 → 10 件見えるようになった
- 2026-08-30 [キャラタブを自動保存にする](adr/006-ui-design-system.md): 未保存の変更が**黙って消えていた**のを直した(エタの意志を変えてダメージ計算タブへ行き戻ると値が戻る。`CharsPage` の `{#key character.id}` と `App` の `{#key app.tab}` で Workspace ごと再生成され draft が捨てられていた)。600ms debounce の自動保存にし、「保存」ボタンと「未保存」バッジを押せない状態表示へ置換。`onDestroy` で未送信分を flush する。あわせて**この編集で変更した補正源**を左のリストにドットで出す(`equipment` と `randomOption` が同じ `EquipmentPart` を共有していて draft 差分では切り分けられないので、変更時に開いていた `openSource` を記録する)。独立レビューでデータ消失が 3 件出て作り直している — 保存の IPC 完了前に再マウントすると古い draft が保存済みの値を巻き戻す / 保存中に発火した debounce が捨てられ `initialSnapshot` が未送信分まで「保存済み」にする / 名前が空のあいだの編集が理由も出ずに全部消える。未確定 draft をキャラ id ごとにモジュールスコープへ退避し、`initialSnapshot` は実際に送信した瞬間のスナップショットにした。`app.sim`(試し変更)は従来どおり破棄する(保存時点のスナップショットなので残すと新しい保存を古い値で上書きできる)が、破棄したときだけ 1 回知らせる
